---
phase: 13-user-roles-and-permissions
plan: 02
type: execute
depends_on: ["13-01"]
files_modified: [src/App.tsx, src/components/Sidebar.tsx, src/pages/KnowledgeBase.tsx, src/components/FileDetailPanel.tsx]
---

<objective>
Apply role-based UI gating across the application: hide navigation links, restrict routes, and hide CRUD controls based on user role.

Purpose: Each role sees a tailored, complete-feeling app with no broken buttons or "access denied" pages. Hidden elements leave no visual gaps.
Output: Sidebar, routes, and Knowledge Base CRUD controls respect user roles seamlessly.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-user-roles-and-permissions/13-CONTEXT.md
@.planning/phases/13-user-roles-and-permissions/13-01-SUMMARY.md

@src/App.tsx
@src/components/Sidebar.tsx
@src/pages/KnowledgeBase.tsx
@src/components/FileDetailPanel.tsx
@src/hooks/usePermissions.ts

**Constraining decisions:**
- Phase 9: Purple theme #8466e4, Russian localization throughout
- Phase 12: FileDetailPanel extracted as standalone component with props
- CONTEXT.md: Hidden elements must not leave awkward gaps. Each role feels like a complete app.

**Role access matrix (from CONTEXT.md):**
| Feature | administrator | moderator | user |
|---------|--------------|-----------|------|
| Chat page | Yes | Yes | Yes |
| Knowledge Base (browse/download) | Yes | Yes | Yes |
| Knowledge Base (create/upload/edit/delete) | Yes | Yes | No |
| Settings page (Portainer) | Yes | No | No |
| Sidebar: Settings link | Yes | No | No |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate Sidebar navigation and Settings route by role</name>
  <files>src/components/Sidebar.tsx, src/App.tsx</files>
  <action>
  **Sidebar.tsx:**
  1. Import `usePermissions` from `@/hooks/usePermissions`
  2. Call `const { canAccessSettings } = usePermissions()` inside the Sidebar component
  3. Wrap the Settings navigation button (lines 84-94, the `<Button>` with `<Link to="/settings">`) in a conditional: only render if `canAccessSettings` is true
  4. Do NOT gray out or disable the link - remove it entirely from the DOM when not authorized (per CONTEXT.md: "just gone")
  5. The remaining nav items (Chat, Knowledge Base) should look natural without the Settings link. Since they use `flex-col gap-2`, removing one item won't leave a visual gap.

  **App.tsx:**
  1. Import `UserRole` from `@/hooks/usePermissions`
  2. Add `allowedRoles={['administrator'] as UserRole[]}` to the ProtectedRoute wrapping the `/settings` route (line 57)
  3. This ensures direct URL navigation to `/settings` by non-administrators silently redirects to `/chat`
  4. Chat and Knowledge Base routes remain unchanged (no allowedRoles = any authenticated user)
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit`. Sidebar conditionally renders Settings link. Settings route has allowedRoles restriction.</verify>
  <done>Settings link hidden in Sidebar for moderator/user roles. Direct URL /settings redirects non-administrators to /chat. Chat and Knowledge Base accessible to all roles.</done>
</task>

<task type="auto">
  <name>Task 2: Gate Knowledge Base CRUD controls for user role</name>
  <files>src/pages/KnowledgeBase.tsx, src/components/FileDetailPanel.tsx</files>
  <action>
  **KnowledgeBase.tsx:**
  1. Import `usePermissions` from `@/hooks/usePermissions`
  2. Call `const { canManageFiles } = usePermissions()`
  3. Conditionally render the Create Folder button (lines 172-179, the `<Button>` with `<FolderPlus>` icon in the folder sidebar):
     - Only render if `canManageFiles` is true
     - When hidden, the "Структура каталогов" header should still look normal. The `flex items-center justify-between` will just center the text without the button.
  4. Conditionally render the Upload File button (lines 209-217, the `<Button>` with `<FilePlus>` icon and "Добавить" text):
     - Only render if `canManageFiles` is true
     - The search bar will expand to fill the space naturally due to `flex-1`
  5. Conditionally render the Delete Confirmation Dialog (lines 392-420):
     - Only render if `canManageFiles` is true (won't be triggered anyway, but clean DOM)
  6. The grid card download buttons (lines 332-360) remain visible for ALL roles - download is allowed for everyone
  7. The `onDelete` prop passed to FileDetailPanel (line 387) should only be provided when `canManageFiles` is true. Pass `undefined` or a no-op when user role.

  **FileDetailPanel.tsx:**
  1. Import `usePermissions` from `@/hooks/usePermissions`
  2. Call `const { canManageFiles } = usePermissions()`
  3. In the non-editing action buttons section (lines 291-321):
     - The Download button (lines 292-303) stays visible for ALL roles
     - The `<div className="flex gap-3">` containing Edit and Delete buttons (lines 304-320): only render if `canManageFiles` is true
     - When hidden, the Download button alone with `w-full` fills the width naturally
  4. The editing state (lines 267-289) doesn't need guarding because `handleEdit` can only be triggered from the Edit button which is already hidden.
  5. Do NOT add disabled states or tooltips for missing permissions. Controls are simply absent per CONTEXT.md.
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit`. `npm run build` succeeds. CRUD buttons hidden when canManageFiles is false. Download remains visible for all roles.</verify>
  <done>User role sees Knowledge Base without create/upload/edit/delete controls. Administrator and moderator see full CRUD. No visual gaps or broken layouts. Download works for all roles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Role-based UI gating across the entire application: Sidebar navigation, route protection, and Knowledge Base CRUD controls are hidden/shown based on the user's frontend_role from Directus.</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. **Test Administrator role:**
       - Log in as an administrator user
       - Verify: Sidebar shows all 3 links (ИИ Чат, Докумненты, Настройки)
       - Verify: Settings page loads with Portainer monitoring
       - Verify: Knowledge Base shows Create Folder, Upload, Edit, Delete buttons
    3. **Test Moderator role:**
       - Log in as a moderator user
       - Verify: Sidebar shows only 2 links (ИИ Чат, Докумненты) - NO Настройки
       - Verify: Navigating to /settings in URL bar redirects to /chat
       - Verify: Knowledge Base shows Create Folder, Upload, Edit, Delete buttons (same as admin)
    4. **Test User role:**
       - Log in as a user role account
       - Verify: Sidebar shows only 2 links (ИИ Чат, Докумненты) - NO Настройки
       - Verify: Navigating to /settings in URL bar redirects to /chat
       - Verify: Knowledge Base shows ONLY browsing and download (NO Create Folder, Upload, Edit, Delete)
       - Verify: File detail panel shows ONLY Download button (no Edit/Delete)
    5. **All roles:** Chat page works normally with both RAG and LLM modes
    6. **Layout check:** No visual gaps, broken layouts, or empty spaces where hidden elements were
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npx tsc --noEmit` passes without errors
- [ ] `npm run build` succeeds
- [ ] Administrator sees all features
- [ ] Moderator sees everything except Settings
- [ ] User sees everything except Settings and file CRUD
- [ ] Direct URL access to restricted pages redirects properly
- [ ] No visual gaps in UI for any role
- [ ] Human verification approved
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors introduced
- Three-tier role hierarchy works: administrator > moderator > user
- Hidden elements leave no visual gaps
- Direct URL navigation enforced via ProtectedRoute
- Human verification confirms all three roles work correctly
- Phase 13 complete
</success_criteria>

<output>
After completion, create `.planning/phases/13-user-roles-and-permissions/13-02-SUMMARY.md`
</output>
