---
phase: 09-ui-overhaul-taks-will-specify-later
plan: 05
type: execute
depends_on: []
files_modified: [src/components/CreateFolderDialog.tsx, src/components/UploadFileDialog.tsx]
---

<objective>
Redesign folder creation and file upload dialogs to match Folder_add.png design with purple buttons and clean styling.

Purpose: Create polished, consistent modal dialogs matching final design system
Output: Dialogs with clean white backgrounds, proper spacing, purple action buttons, and modern input styling
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ui-overhaul-taks-will-specify-later/09-CONTEXT.md
@src/components/CreateFolderDialog.tsx
@src/components/UploadFileDialog.tsx
@USER_IMPUT_FILES/Design/Knowledgebase/Folder_add.png

**Purple theme color:** #8466e4 (for action buttons)
**Design reference:** USER_IMPUT_FILES/Design/Knowledgebase/Folder_add.png

**Key design elements from Folder_add.png:**
- Clean white modal background
- Centered dialog with proper shadow
- Title: "Создать новую папку" in semibold
- Input fields with clean borders, rounded corners
- Labels above inputs
- Dropdown/Select for parent folder with proper styling
- Action buttons:
  - Cancel: outline variant, gray text
  - Create ("Создать"): purple background (#8466e4), white text
- Proper spacing between elements (gap-4 or gap-6)
- Close X button in top-right corner

**Current state:**
- CreateFolderDialog has basic shadcn dialog structure
- Uses default button styling (needs purple for primary action)
- UploadFileDialog likely follows similar pattern
- Both need visual polish to match design system

**DO NOT:**
- Change dialog functionality (form submission, validation, error handling)
- Modify folder/file creation API calls
- Change state management or data flow
- Add new fields or remove existing ones
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign CreateFolderDialog to match design</name>
  <files>src/components/CreateFolderDialog.tsx</files>
  <action>
  Transform CreateFolderDialog to match Folder_add.png design.

  Dialog styling:
  - DialogContent: ensure clean white background (bg-white)
  - Max width: sm:max-w-[500px] (slightly wider for comfort)
  - Shadow: default shadcn shadow is good
  - Padding: p-6 or consistent with design

  Dialog header:
  - DialogTitle: "Создать новую папку" (Russian, matching design)
  - Font: text-xl font-semibold
  - DialogDescription: keep current or simplify to match design

  Form inputs:
  1. Folder name input:
     - Label: "Название папки" (Russian)
     - Input: rounded-md border-gray-300
     - Focus: focus:ring-2 focus:ring-[#8466e4] focus:border-[#8466e4] (purple)
     - Placeholder: "Введите название..." (Russian)

  2. Parent folder select:
     - Label: "Родительская папка" (Russian)
     - Select: clean dropdown styling
     - Default option: "Root (No Parent)" or "Корневая папка"
     - Options: list of folders

  Spacing:
  - Gap between fields: gap-4 or gap-6
  - Padding: py-4 or py-6 for form section

  Action buttons (DialogFooter):
  - Cancel button:
    * variant="outline"
    * Text: "Отмена" (Russian)
    * Colors: default outline (gray)
  - Create button:
    * type="submit"
    * Background: bg-[#8466e4] (purple)
    * Hover: hover:bg-[#7049f3] (darker purple)
    * Text: "Создать" (white, Russian)
    * Icon: optional FolderPlus icon
    * Disabled state: opacity-50 cursor-not-allowed

  Keep all existing functionality: form submission, validation, error display, loading states, onOpenChange behavior.

  DO NOT change props, mutation logic, or error handling. ONLY update visual styling and text labels.
  </action>
  <verify>CreateFolderDialog displays with purple "Создать" button, clean inputs, Russian labels</verify>
  <done>Folder dialog styled to match Folder_add.png: purple action button, clean layout, Russian text, proper spacing.</done>
</task>

<task type="auto">
  <name>Task 2: Apply same design pattern to UploadFileDialog</name>
  <files>src/components/UploadFileDialog.tsx</files>
  <action>
  Apply consistent design pattern to UploadFileDialog, following CreateFolderDialog styling.

  Dialog structure (if not already similar):
  - Clean white background
  - Proper max-width and padding
  - Title: "Загрузить файл" (Russian)
  - Description: brief explanation if needed

  Form inputs:
  1. File picker:
     - Label: "Выбрать файл" or "Файл"
     - Input type="file" with clean styling
     - Border: border-gray-300 rounded-md
     - Focus: purple ring

  2. Title/name input (if exists):
     - Label: "Название" (Russian)
     - Input styling consistent with folder dialog
     - Purple focus ring

  3. Description textarea (if exists):
     - Label: "Описание" (Russian)
     - Textarea styling consistent
     - Purple focus ring

  4. Folder selection dropdown (to specify upload destination):
     - Label: "Папка" (Russian)
     - Select with folder list
     - Consistent with parent folder select in CreateFolderDialog

  Action buttons:
  - Cancel: outline variant, "Отмена" (Russian)
  - Upload/Add:
    * Background: bg-[#8466e4] (purple)
    * Hover: hover:bg-[#7049f3]
    * Text: "Загрузить" or "Добавить" (white, Russian)
    * Icon: optional Upload or Plus icon

  Error display:
  - Text: text-red-500 text-sm
  - Positioned below relevant input

  Loading states:
  - Button: disabled with Loader2 icon spinning
  - Consistent with folder dialog loading

  Keep all existing file upload logic, validation, multipart form handling, metadata updates. ONLY update visual styling and text labels.

  DO NOT change upload functionality, file handling, or API integration. ONLY styling and labels.
  </action>
  <verify>UploadFileDialog styled consistently with CreateFolderDialog, purple action button, Russian labels</verify>
  <done>File upload dialog matches design pattern: purple action button, clean inputs, Russian text, consistent spacing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Folder creation and file upload dialogs redesigned with purple theme and clean styling</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Navigate to /knowledge-base page
    3. Test CreateFolderDialog:
       - Click folder creation button (Plus icon in sidebar)
       - Dialog opens with clean white background
       - Title: "Создать новую папку" in Russian
       - Input field: "Название папки" label
       - Dropdown: "Родительская папка" label with folder list
       - Cancel button: outline style, "Отмена" text
       - Create button: purple (#8466e4), "Создать" text
       - Focus on input shows purple ring
       - Submit form - verify functionality still works
       - Close dialog - verify it closes properly
    4. Test UploadFileDialog:
       - Select a folder, click "Добавить" button
       - Dialog opens with clean layout
       - Title: "Загрузить файл" in Russian
       - File input with proper styling
       - Other inputs (title, description, folder) with Russian labels
       - Cancel button: outline, "Отмена"
       - Upload button: purple (#8466e4), "Загрузить" or "Добавить"
       - Focus states show purple ring
       - Upload a file - verify functionality works
       - Error handling still works (try invalid file)
    5. Visual consistency:
       - Both dialogs follow same design pattern
       - Purple action buttons consistent (#8466e4)
       - Input styling identical across dialogs
       - Spacing and padding match design
       - Russian labels throughout
    6. Compare with Folder_add.png:
       - CreateFolderDialog matches reference design
       - Clean, modern appearance
       - Proper shadows and spacing
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] CreateFolderDialog displays with purple "Создать" button
- [ ] UploadFileDialog displays with purple action button
- [ ] Both dialogs use Russian labels
- [ ] Input focus states show purple ring
- [ ] All form functionality preserved (validation, submission, errors)
- [ ] Visual design matches Folder_add.png pattern
</verification>

<success_criteria>

- All tasks completed
- CreateFolderDialog matches Folder_add.png design
- UploadFileDialog follows same design pattern
- Purple action buttons (#8466e4) on both dialogs
- Russian labels throughout ("Создать", "Отмена", "Загрузить", etc.)
- Clean white backgrounds with proper spacing
- Input focus states show purple rings
- Cancel buttons use outline variant
- All dialog functionality preserved (folder creation, file upload, validation, errors)
- No errors or warnings introduced
- Phase 9 complete - all 5 visual overhaul plans done
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-overhaul-taks-will-specify-later/09-05-SUMMARY.md`:

# Phase 9 Plan 5: Dialog Components Redesign Summary

**Folder and file upload dialogs redesigned with purple theme and clean modern styling**

## Accomplishments

- CreateFolderDialog styled to match Folder_add.png design
- UploadFileDialog follows consistent design pattern
- Purple action buttons on both dialogs (#8466e4)
- Russian labels throughout dialogs
- Clean white backgrounds with proper spacing
- Input focus states show purple rings
- Cancel buttons use outline variant
- All form functionality preserved

## Files Created/Modified

- `src/components/CreateFolderDialog.tsx` - Purple button, Russian labels, clean styling
- `src/components/UploadFileDialog.tsx` - Consistent design pattern applied

## Decisions Made

Dialog text labels in Russian:
- Create: "Создать"
- Cancel: "Отмена"
- Upload: "Загрузить" or "Добавить"
- Folder name: "Название папки"
- Parent folder: "Родительская папка"

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 9 complete - all UI overhaul plans finished. Ready for UAT verification.
</output>
