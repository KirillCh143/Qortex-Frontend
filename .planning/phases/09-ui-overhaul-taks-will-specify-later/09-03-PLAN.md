---
phase: 09-ui-overhaul-taks-will-specify-later
plan: 03
type: execute
depends_on: []
files_modified: [src/pages/KnowledgeBase.tsx]
---

<objective>
Transform Knowledge Base grid view to match Tiles.png design with file icons, selection checkboxes, and purple accents.

Purpose: Create visually polished tile-based document browser matching final design
Output: Grid view with colorful file icon cards, checkboxes, metadata, and purple "Добавить" button
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ui-overhaul-taks-will-specify-later/09-CONTEXT.md
@src/pages/KnowledgeBase.tsx
@USER_IMPUT_FILES/Design/Knowledgebase/Tiles.png

**Purple theme color:** #8466e4 (for buttons and active states)
**Design reference:** USER_IMPUT_FILES/Design/Knowledgebase/Tiles.png

**Key design elements from Tiles.png:**
- Cards with large file icons (PDF red, DOCX blue, XLSX green backgrounds)
- Selection checkbox in top-right corner of each card
- File name as prominent title
- Metadata below (date modified, uploader name)
- Purple "Добавить" button (top-right of page)
- Clean card layout with proper shadows
- Icon backgrounds: light colored circles (red for PDF, blue for DOCX, green for XLSX, etc.)

**Current state:**
- Grid view shows Card components with title, description, file type badge, size, date
- No selection checkboxes
- No large file icons with colored backgrounds
- "Upload File" button uses default styling
- Need to add file type icon logic and selection UI

**File type to color mapping:**
- PDF: red/pink background (#fee2e2 or similar)
- Word/DOCX: blue background (#dbeafe or similar)
- Excel/XLSX: green background (#dcfce7 or similar)
- TXT/MD: gray background (#f3f4f6 or similar)
- Default: gray background

**DO NOT:**
- Implement actual selection state management (checkboxes are visual only for this phase)
- Change file loading or download functionality
- Modify folder tree or list view (separate plan)
- Change API integration
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign grid cards with file icons and checkboxes</name>
  <files>src/pages/KnowledgeBase.tsx</files>
  <action>
  Transform grid view cards in KnowledgeBase.tsx to match Tiles.png design (around line 164-189, the grid view section).

  Card structure changes:
  1. Add checkbox in top-right corner:
     - Use Checkbox component from shadcn (import from @/components/ui/checkbox)
     - Position: absolute top-3 right-3
     - For now, keep unchecked (no state management in this phase)
     - Add onClick={(e) => e.stopPropagation()} to prevent card click

  2. Large file icon with colored background:
     - Create icon container: circular or rounded-square background
     - Size: w-16 h-16 or w-20 h-20 (prominent, matches design)
     - Background color based on file type:
       * PDF: bg-red-100 with red icon
       * Word (DOCX, DOC): bg-blue-100 with blue icon
       * Excel (XLSX, XLS): bg-green-100 with green icon
       * Text (TXT, MD): bg-gray-100 with gray icon
       * Default: bg-gray-100 with gray icon
     - Use lucide-react icons: FileText for PDF, FileType for DOCX, Sheet for XLSX, File for default
     - Icon color matches background shade (text-red-500, text-blue-500, etc.)

  3. Card layout:
     - File icon at top-center or left
     - File name (title) below icon as prominent text (font-semibold, text-lg)
     - Metadata below: "Изменено {date} • {uploader}"
     - Remove or minimize description (design shows minimal text)
     - Remove type badge (icon conveys type now)

  4. Card styling:
     - Clean white background
     - Border: border border-gray-200
     - Hover: hover:border-[#8466e4] (purple border on hover)
     - Shadow: shadow-sm
     - Padding: p-4 or p-6
     - Cursor: cursor-pointer

  Extract file type from file.type string (e.g., "application/pdf" → "pdf"). Create helper function if needed to map MIME type to display type and color.

  DO NOT change grid container structure, keep same responsive grid (grid-cols-1 md:grid-cols-2). ONLY modify card internals.
  </action>
  <verify>Grid view shows cards with large file icons, colored backgrounds by type, checkboxes in top-right corner</verify>
  <done>Grid cards display file icons with type-based colors, checkboxes visible, file names prominent, metadata shown. Hover state shows purple border.</done>
</task>

<task type="auto">
  <name>Task 2: Update action buttons with purple styling</name>
  <files>src/pages/KnowledgeBase.tsx</files>
  <action>
  Update Upload File button and view toggle buttons to use purple theme.

  Upload File button (around line 117-124):
  - Text: change to "Добавить" (matches Russian design)
  - Background: bg-[#8466e4] (purple)
  - Hover: hover:bg-[#7049f3] (darker purple)
  - Text: white
  - Icon: Plus or Upload icon in white
  - Keep disabled state logic (disabled when no folder selected)

  View toggle buttons (Grid/List around line 128-146):
  - Active state: border-[#8466e4] bg-[#8466e4]/10 text-[#8466e4] (purple theme, not cyan)
  - Inactive state: border-gray-300 text-gray-700
  - Icons: LayoutGrid for grid, List for list
  - Keep current click handlers and view mode switching

  Ensure buttons have proper border-radius (rounded-md or rounded-lg) and padding to match design aesthetic.

  DO NOT change button functionality or disabled states. ONLY update colors and text.
  </action>
  <verify>Upload button is purple with "Добавить" text, view toggles use purple for active state</verify>
  <done>"Добавить" button purple (#8466e4), view toggle buttons have purple active state, all styling polished.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Knowledge Base grid view redesigned with file icons, checkboxes, and purple theme</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Navigate to /knowledge-base page
    3. Ensure grid view is selected (click Grid toggle if needed)
    4. Check grid cards:
       - Each card shows large file icon with colored background
       - PDF files have red/pink background (#fee2e2 or similar)
       - Word files have blue background (#dbeafe or similar)
       - Excel files have green background (#dcfce7 or similar)
       - Checkbox appears in top-right corner of each card
       - File name is prominent (large, semibold)
       - Metadata shows below: date and uploader info
       - Cards have clean white background with subtle shadow
    5. Check hover states:
       - Hovering card shows purple border (#8466e4)
       - Card remains clickable to open detail panel
    6. Check action buttons:
       - "Добавить" button is purple (#8466e4)
       - View toggle buttons: active one has purple accent
       - Both Grid and List toggles work (switch between views)
    7. Compare with Tiles.png:
       - Overall layout matches reference design
       - Colors and spacing are consistent
       - File icons and checkboxes positioned correctly
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Grid cards show file icons with type-based colored backgrounds
- [ ] Checkboxes appear in top-right of cards
- [ ] "Добавить" button is purple
- [ ] View toggles use purple for active state
- [ ] Card hover shows purple border
- [ ] Layout matches Tiles.png design
</verification>

<success_criteria>

- All tasks completed
- Grid view matches Tiles.png design
- File icons with colored backgrounds (red PDF, blue DOCX, green XLSX, gray default)
- Selection checkboxes visible in cards
- "Добавить" button purple with Russian text
- View toggle purple active state
- Card hover states show purple border
- All existing functionality preserved (detail panel, download, folder filtering)
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-overhaul-taks-will-specify-later/09-03-SUMMARY.md`:

# Phase 9 Plan 3: Knowledge Base Grid View Summary

**Grid view transformed with file icons, checkboxes, and purple theme**

## Accomplishments

- Grid cards redesigned with large file icons
- File type-based colored backgrounds (red PDF, blue DOCX, green XLSX)
- Selection checkboxes added to card top-right corners
- "Добавить" button styled with purple theme
- View toggle buttons use purple active state
- Card hover shows purple border
- Layout matches Tiles.png reference design

## Files Created/Modified

- `src/pages/KnowledgeBase.tsx` - Grid view redesign, button styling

## Decisions Made

File type color mapping:
- PDF: red/pink background (#fee2e2)
- Word: blue background (#dbeafe)
- Excel: green background (#dcfce7)
- Text/Default: gray background (#f3f4f6)

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 09-04-PLAN.md (Knowledge Base List View)
</output>
