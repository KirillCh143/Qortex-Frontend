---
phase: 09-ui-overhaul-taks-will-specify-later
plan: 01
type: execute
depends_on: []
files_modified: [src/components/Sidebar.tsx, src/components/Header.tsx]
---

<objective>
Move user section from header to sidebar bottom with initials display and full name.

Purpose: Match design system with user controls in sidebar for better visual hierarchy
Output: Sidebar with user section at bottom, header simplified to page title only
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ui-overhaul-taks-will-specify-later/09-CONTEXT.md
@src/components/Sidebar.tsx
@src/components/Header.tsx
@src/contexts/AuthContext.tsx

**Purple theme color:** #8466e4 (consistent across all Phase 9 plans)

**User data structure:**
- user.first_name (e.g., "Иван")
- user.last_name (e.g., "Петров")
- Initials: First letter of first_name + first letter of last_name, uppercase (e.g., "ИП")

**Current state:**
- User avatar and logout button are in Header component
- Sidebar has navigation items only
- Need to move user section to bottom of sidebar

**Design requirements:**
- User section at bottom of sidebar (after navigation items)
- Avatar shows initials (calculated from first_name + last_name)
- Display full name next to avatar: "{first_name} {last_name}"
- Logout button next to user info
- Purple accent color for active states
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add user section to sidebar bottom</name>
  <files>src/components/Sidebar.tsx</files>
  <action>
  Import useAuth hook to access user data. Add user section at bottom of sidebar after navigation items, before closing flex container div.

  Structure:
  - Separator before user section
  - Flex container with items-center, gap-3, padding
  - Avatar component with initials: Extract first character of user.first_name and user.last_name, convert to uppercase, combine (e.g., "ИП")
  - Avatar background: purple gradient (bg-gradient-to-br from-[#8466e4] to-[#7049f3])
  - Text container with user's full name: "{user.first_name} {user.last_name}" in text-sm font-medium
  - Logout button with LogOut icon

  Ensure sidebar flex-col with justify-between so user section stays at bottom even with few nav items.

  DO NOT change navigation items, branding, or existing functionality. ONLY add the user section at bottom.
  </action>
  <verify>npm run build succeeds, sidebar displays user section at bottom with correct initials and full name</verify>
  <done>Sidebar shows user avatar with initials, full name, and logout button at bottom. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Remove user section from header</name>
  <files>src/components/Header.tsx</files>
  <action>
  Simplify Header component to show only the page title. Remove the entire user section (Avatar, Button, logout handler, and the right-side flex container).

  Keep:
  - Header container with border-b
  - Page title (h1 with title prop)

  Remove:
  - useAuth hook import and usage
  - useNavigate hook import and usage
  - handleLogout function
  - Avatar component and AvatarFallback import
  - Button component usage for logout
  - LogOut icon import
  - Entire "User section (right)" div

  Header should now be a simple component that just displays the page title. DO NOT add new functionality, ONLY remove the user section.
  </action>
  <verify>npm run build succeeds, header shows only page title, no user controls</verify>
  <done>Header simplified to title-only display. User controls fully removed. No TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>User section moved from header to sidebar bottom with initials and full name display</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Login to the application
    3. Check sidebar:
       - User section appears at bottom of sidebar
       - Avatar shows correct initials (first letter of first name + first letter of last name)
       - Full name displays next to avatar: "{first_name} {last_name}"
       - Logout button is present and accessible
       - Purple gradient background on avatar matches design (#8466e4)
    4. Check header:
       - Only page title is displayed
       - No user avatar or logout button in header
    5. Test logout functionality:
       - Click logout button in sidebar
       - Redirects to login page
       - Session cleared properly
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Sidebar shows user section at bottom with initials and full name
- [ ] Header simplified to title-only
- [ ] Logout functionality works from sidebar
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- User section successfully moved from header to sidebar
- Initials calculated and displayed correctly
- Full name displayed next to avatar
- Logout button works from sidebar location
- Header shows only page title
- Purple theme maintained (#8466e4)
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-overhaul-taks-will-specify-later/09-01-SUMMARY.md`:

# Phase 9 Plan 1: Sidebar User Section Redesign Summary

**User controls moved from header to sidebar bottom with initials display**

## Accomplishments

- User section relocated to bottom of sidebar
- Avatar displays initials from first_name + last_name
- Full name shown next to avatar
- Logout button accessible in sidebar
- Header simplified to title-only display

## Files Created/Modified

- `src/components/Sidebar.tsx` - Added user section at bottom
- `src/components/Header.tsx` - Removed user controls, title-only

## Decisions Made

None

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 09-02-PLAN.md (Chat Page Visual Overhaul)
</output>
