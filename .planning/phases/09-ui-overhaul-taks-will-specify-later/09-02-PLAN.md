---
phase: 09-ui-overhaul-taks-will-specify-later
plan: 02
type: execute
depends_on: []
files_modified: [src/pages/Chat.tsx, src/components/MessageBubble.tsx, src/components/ChatInput.tsx]
---

<objective>
Transform chat page to match Chat.png design with purple user messages, clean layout, and modern input.

Purpose: Create polished, professional chat interface matching final design mockups
Output: Chat page with purple theme, clean message bubbles, modern input with purple send button
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ui-overhaul-taks-will-specify-later/09-CONTEXT.md
@src/pages/Chat.tsx
@src/components/MessageBubble.tsx
@src/components/ChatInput.tsx
@USER_IMPUT_FILES/Design/Chat/Chat.png

**Purple theme color:** #8466e4 (primary action color)
**Design reference:** USER_IMPUT_FILES/Design/Chat/Chat.png

**Key design elements from Chat.png:**
- Clean white background
- Purple user messages (#8466e4) with rounded-2xl
- Light gray assistant messages with bot icon
- Modern input field with placeholder text
- Purple send button (circular or rounded)
- Tab-based mode switcher at top (База знаний | Gemini)
- Clean, spacious layout with good padding

**Current state:**
- User messages use cyan color (#06b6d4)
- Mode toggle uses cyan accent
- Send button uses cyan
- Need to replace all cyan with purple (#8466e4)
- Message bubbles need design polish

**DO NOT:**
- Change functionality or backend logic
- Modify markdown rendering behavior
- Remove existing features (mode switcher, clear history, etc.)
- Change message flow or state management
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update message bubble styling to match design</name>
  <files>src/components/MessageBubble.tsx</files>
  <action>
  Transform message bubbles to match Chat.png design exactly.

  User messages:
  - Background: #8466e4 (purple, not cyan)
  - Text: white
  - Border-radius: rounded-2xl (more rounded than current)
  - Padding: px-4 py-3 (slightly more generous)
  - Keep flex-row-reverse alignment (user messages on right)

  Assistant messages:
  - Background: #f3f4f6 (light gray)
  - Text: gray-900
  - Border-radius: rounded-2xl
  - Padding: px-4 py-3
  - Bot icon in gray circular background
  - Keep flex-row alignment (assistant messages on left)

  Avatar styling:
  - User avatar: bg-[#8466e4] (purple) with white icon
  - Assistant avatar: bg-gray-200 with gray-700 icon
  - Size: h-8 w-8 (keep current)

  Keep all existing markdown rendering logic unchanged. ONLY modify colors and border-radius. DO NOT change the ReactMarkdown component or its renderers.
  </action>
  <verify>Message bubbles display with purple user messages and light gray assistant messages, rounded-2xl corners</verify>
  <done>User messages purple (#8466e4), assistant messages light gray (#f3f4f6), both with rounded-2xl styling. Markdown rendering intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update chat input styling with purple send button</name>
  <files>src/components/ChatInput.tsx</files>
  <action>
  Modernize chat input to match Chat.png design.

  Input field:
  - Keep current textarea structure and auto-resize behavior
  - Border: border-gray-300 (slightly lighter)
  - Focus ring: focus:ring-2 focus:ring-[#8466e4] (purple, not cyan)
  - Rounded: rounded-xl (more rounded)
  - Placeholder: keep current Russian text or update to match design

  Send button:
  - Background: bg-[#8466e4] (purple)
  - Hover: hover:bg-[#7049f3] (darker purple)
  - Icon: white Send icon
  - Shape: rounded-full or rounded-xl (circular or highly rounded)
  - Size: h-10 w-10 or h-10 px-4 (adjust to match design)
  - Active state: slightly darker purple

  Container:
  - Keep border-t border-gray-200
  - Background: white
  - Padding: p-4 (current is good)

  DO NOT change functionality (Enter to send, Shift+Enter for new line, auto-resize, disabled states). ONLY update colors and styling.
  </action>
  <verify>Chat input has purple focus ring, send button is purple circular/rounded with white icon</verify>
  <done>Input field styled with purple focus ring. Send button purple (#8466e4) with white icon. All functionality preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Update mode toggle and page styling</name>
  <files>src/pages/Chat.tsx</files>
  <action>
  Update Chat page styling to match design.

  Mode toggle buttons (RAG Search / LLM Chat):
  - Active state background: bg-[#8466e4] (purple, not cyan)
  - Active state hover: hover:bg-[#7049f3]
  - Active state text: white
  - Inactive state: outline variant (keep current)
  - Border-radius: rounded-lg
  - Size: keep current (sm)

  Clear History button:
  - Keep outline variant
  - Hover state: hover:border-gray-400
  - Icon and text: keep current

  Page background:
  - Ensure clean white background throughout
  - Messages container: bg-white (if not already)

  Mode toggle container:
  - Keep current border-b structure
  - Background: white or very light gray (bg-gray-50)
  - Padding: keep current

  DO NOT change mode switching logic, clear history functionality, or message loading behavior. ONLY update colors from cyan to purple.
  </action>
  <verify>Mode toggle buttons use purple for active state, page has clean white background</verify>
  <done>Mode toggle styled with purple active state. Page background clean white. All functionality intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Chat page visual overhaul with purple theme matching Chat.png design</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Navigate to /chat page
    3. Check mode toggle:
       - Active tab has purple background (#8466e4)
       - Inactive tab has outline style
       - Switching between RAG/LLM modes works correctly
    4. Send test messages:
       - User messages appear in purple bubbles (#8466e4) on the right
       - Assistant messages appear in light gray bubbles on the left
       - Bot icon shows in gray circle for assistant messages
       - Bubbles have rounded-2xl corners (noticeably rounded)
    5. Check input field:
       - Focus ring is purple when typing
       - Send button is purple (#8466e4)
       - Send button has white Send icon
       - Enter to send works, Shift+Enter for new line works
    6. Test markdown rendering:
       - Send message with **bold**, *italic*, lists, code blocks
       - Verify markdown still renders correctly in assistant messages
    7. Visual polish:
       - Clean white background throughout
       - Proper spacing and padding
       - No cyan colors remaining (all replaced with purple)
       - Layout matches Chat.png reference design
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] User messages display in purple (#8466e4)
- [ ] Assistant messages display in light gray
- [ ] Send button is purple with white icon
- [ ] Mode toggle uses purple for active state
- [ ] No cyan colors remain in chat interface
- [ ] Markdown rendering still works correctly
- [ ] All chat functionality preserved
</verification>

<success_criteria>

- All tasks completed
- Chat page matches Chat.png design
- Purple theme (#8466e4) applied consistently
- User messages purple, assistant messages gray
- Send button purple with white icon
- Mode toggle purple active state
- Clean white background
- All functionality preserved (mode switching, message history, markdown rendering)
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-overhaul-taks-will-specify-later/09-02-SUMMARY.md`:

# Phase 9 Plan 2: Chat Page Visual Overhaul Summary

**Chat interface transformed with purple theme and clean modern design**

## Accomplishments

- User messages styled with purple background (#8466e4)
- Assistant messages with light gray background
- Message bubbles with rounded-2xl corners
- Chat input with purple focus ring
- Purple send button with white icon
- Mode toggle with purple active state
- All cyan colors replaced with purple theme
- Markdown rendering preserved

## Files Created/Modified

- `src/components/MessageBubble.tsx` - Purple user messages, gray assistant messages
- `src/components/ChatInput.tsx` - Purple send button and focus states
- `src/pages/Chat.tsx` - Purple mode toggle active state

## Decisions Made

None

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 09-03-PLAN.md (Knowledge Base Grid View)
</output>
