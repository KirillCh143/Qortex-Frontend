---
phase: 09-ui-overhaul-taks-will-specify-later
plan: 04
type: execute
depends_on: []
files_modified: [src/components/FileListView.tsx]
---

<objective>
Transform Knowledge Base list view to match Lines.png design with file icons, selection checkboxes, and clean table layout.

Purpose: Create polished list/table view for efficient document browsing
Output: Table view with file icons, checkboxes, metadata columns, and proper row styling
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ui-overhaul-taks-will-specify-later/09-CONTEXT.md
@src/components/FileListView.tsx
@USER_IMPUT_FILES/Design/Knowledgebase/Lines.png

**Purple theme color:** #8466e4 (for checkboxes and hover states)
**Design reference:** USER_IMPUT_FILES/Design/Knowledgebase/Lines.png

**Key design elements from Lines.png:**
- Table/list format with rows for each file
- Selection checkbox as first column
- File icon with colored background (same as grid view)
- File name column
- Metadata columns (type, size, date)
- Clean row hover states
- Proper spacing and alignment
- Download icon in last column

**Current state:**
- FileListView component has basic table structure
- No selection checkboxes
- No file icons with colored backgrounds
- Simple text-based type badges
- Need to add icons and checkboxes

**File type to color mapping (same as grid view):**
- PDF: red icon with red background
- Word/DOCX: blue icon with blue background
- Excel/XLSX: green icon with green background
- TXT/MD: gray icon with gray background
- Default: gray icon

**DO NOT:**
- Implement actual selection state management (checkboxes are visual only)
- Change file click behavior or download functionality
- Modify grid view (separate plan)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add checkboxes and file icons to table rows</name>
  <files>src/components/FileListView.tsx</files>
  <action>
  Transform FileListView table to match Lines.png design.

  Table header changes:
  - Add first column for checkbox (narrow, w-12)
  - Add second column for icon (narrow, w-16)
  - Existing columns: Title, Type, Size, Uploaded
  - Keep download icon column at end

  Table structure:
  ```
  <th> checkbox </th>
  <th> icon </th>
  <th> Title </th>
  <th> Type </th>
  <th> Size </th>
  <th> Uploaded </th>
  <th> download </th>
  ```

  Row changes (tbody):
  1. First cell: Checkbox
     - Import Checkbox from @/components/ui/checkbox
     - Centered in cell
     - Unchecked by default (no state management this phase)
     - Add onClick={(e) => e.stopPropagation()} to prevent row click

  2. Second cell: File icon with colored background
     - Container: w-10 h-10 rounded-lg flex items-center justify-center
     - Background color by file type (extract from file.type):
       * PDF: bg-red-100 text-red-500
       * Word: bg-blue-100 text-blue-500
       * Excel: bg-green-100 text-green-500
       * Text: bg-gray-100 text-gray-500
       * Default: bg-gray-100 text-gray-500
     - Icon from lucide-react (FileText, FileType, Sheet, File)
     - Icon size: h-5 w-5

  3. Title cell: Keep current (file.title)
  4. Type cell: Keep badge or simplify to text
  5. Size cell: Keep current (formatFileSize)
  6. Uploaded cell: Keep current (formatDate)
  7. Download icon cell: Keep current

  Row styling:
  - Hover: hover:bg-gray-50 (light gray)
  - Selected state (future): could use bg-[#8466e4]/10 (purple tint, not implemented yet)
  - Border: border-b border-gray-100
  - Cursor: cursor-pointer
  - Padding: px-4 py-3

  Create helper function to map file MIME type to icon and color (can reuse logic from Plan 03 if implemented there, or create new).

  DO NOT change table container, onSelectFile callback, or row click behavior. ONLY add checkboxes and icons, update styling.
  </action>
  <verify>Table rows show checkbox in first column, file icon with colored background in second column</verify>
  <done>List view displays checkboxes and file icons with type-based colors. Rows properly formatted. All columns aligned.</done>
</task>

<task type="auto">
  <name>Task 2: Polish table styling and hover states</name>
  <files>src/components/FileListView.tsx</files>
  <action>
  Enhance table visual polish to match Lines.png design.

  Table header styling:
  - Background: bg-gray-50 (light gray, not gray-100)
  - Border: border-b border-gray-200
  - Text: text-sm font-semibold text-gray-700
  - Padding: px-4 py-3
  - Alignment: text-left for most columns, text-center for checkbox and icon columns

  Table row hover:
  - Background: hover:bg-gray-50
  - Transition: transition-colors duration-150
  - Subtle lift or border effect on hover (optional, if matches design)

  Row selected state (visual preparation for future):
  - Add conditional class structure for future selection state
  - Would use: bg-[#8466e4]/10 border-l-4 border-[#8466e4] when selected
  - Not active in this phase, just prepare class structure

  Typography consistency:
  - Title: text-sm font-medium text-gray-900
  - Metadata (type, size, date): text-sm text-gray-600
  - Icons: text-gray-400 or type-specific color

  Ensure table is scrollable if content overflows (overflow-x-auto on container).

  DO NOT change table data structure or functionality. ONLY update visual styling (colors, spacing, hover states).
  </action>
  <verify>Table has polished styling with proper hover states, colors match Lines.png design</verify>
  <done>Table styling polished with light gray header, proper hover states, consistent typography. Visual design matches Lines.png.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Knowledge Base list view redesigned with checkboxes, file icons, and clean table styling</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Navigate to /knowledge-base page
    3. Switch to List view (click List toggle button)
    4. Check table structure:
       - First column has checkboxes (one per row)
       - Second column has file icons with colored backgrounds
       - Icons match file type (red PDF, blue DOCX, green XLSX, gray others)
       - Title column shows file names
       - Type, Size, Uploaded columns show metadata
       - Download icon in last column
    5. Check row interactions:
       - Hovering row shows light gray background
       - Clicking row opens detail panel (existing behavior)
       - Clicking checkbox does NOT open detail panel (stopPropagation works)
    6. Check visual polish:
       - Table header has light gray background
       - Column headers are semibold
       - Proper spacing and padding throughout
       - Icons are sized correctly and centered
       - Colors match file types consistently
    7. Compare with Lines.png:
       - Overall layout matches reference design
       - Checkboxes and icons positioned correctly
       - Typography and colors are consistent
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Table shows checkbox and icon columns
- [ ] File icons have type-based colored backgrounds
- [ ] Row hover shows light gray background
- [ ] All columns properly aligned and formatted
- [ ] Layout matches Lines.png design
</verification>

<success_criteria>

- All tasks completed
- List view matches Lines.png design
- Checkboxes in first column (visual only, no state)
- File icons in second column with colored backgrounds
- Table header styled with light gray background
- Row hover states work correctly
- Typography and spacing polished
- All existing functionality preserved (row clicks, detail panel, download)
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-overhaul-taks-will-specify-later/09-04-SUMMARY.md`:

# Phase 9 Plan 4: Knowledge Base List View Summary

**List view transformed with checkboxes, file icons, and clean table styling**

## Accomplishments

- Table structure enhanced with checkbox and icon columns
- File icons with type-based colored backgrounds (red PDF, blue DOCX, green XLSX)
- Selection checkboxes added to first column
- Table header styled with light gray background
- Row hover states polished
- Typography and spacing improved
- Layout matches Lines.png reference design

## Files Created/Modified

- `src/components/FileListView.tsx` - Table structure, checkboxes, icons, styling

## Decisions Made

File type icon mapping (consistent with Plan 03):
- PDF: FileText icon, red background
- Word: FileType icon, blue background
- Excel: Sheet icon, green background
- Text/Default: File icon, gray background

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 09-05-PLAN.md (Dialog Components Redesign)
</output>
