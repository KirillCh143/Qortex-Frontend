---
phase: 03-authentication-system
plan: 02
type: execute
depends_on: ["03-01"]
files_modified: [src/pages/Login.tsx, src/App.tsx]
---

<objective>
Build custom login page with Directus authentication integration.

Purpose: Provide user-facing interface for authentication with proper error handling and validation.
Output: Fully functional custom login page that authenticates users via Directus and redirects to protected routes.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-authentication-system/DISCOVERY.md
@.planning/phases/03-authentication-system/03-01-SUMMARY.md
@src/contexts/AuthContext.tsx
@src/App.tsx

**Tech stack available:**
- Shadcn/UI components (Button, Input, Separator installed)
- Lucide React icons
- React Router DOM with useNavigate
- AuthContext from Plan 03-01

**Established patterns:**
- Shadcn/UI for consistent component styling
- Tailwind CSS for layout (color palette: deep-blue-900 primary, blue-600 secondary, cyan-400 accent)
- Path aliases (@/ for src/)

**Constraining decisions:**
- Custom login page (not Directus default interface) - from roadmap
- Mock-first approach deferred to Phase 7 (build real auth first, toggle later)
- localStorage session management via AuthContext

**From Plan 03-01:**
- AuthContext provides: login(email, password), logout(), user, loading, isAuthenticated
- login() throws errors that need handling (401 invalid credentials, 404 user not found)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create custom login page with form validation</name>
  <files>src/pages/Login.tsx</files>
  <action>
    Create src/pages/Login.tsx implementing the login UI pattern from DISCOVERY.md Section 6.3.

    Requirements:
    - Use Shadcn Button and Input components (if Input not installed, install it first with npx shadcn@latest add input)
    - Form with email (type="email", required) and password (type="password", required) inputs
    - Submit button with loading state (disabled during login)
    - useAuth() hook for login method access
    - useNavigate() from react-router-dom for redirect after successful login
    - Local state for email, password, error message, and submitting boolean

    Layout styling:
    - Centered card layout on full-screen background
    - Company branding at top (Building2 icon + "Company Name" from Sidebar pattern)
    - Use deep-blue-900 background, white card with shadow
    - Apply color palette (cyan-400 for accents, blue-600 for buttons)

    DO NOT implement password reset or registration - out of scope for Phase 3.
  </action>
  <verify>TypeScript compilation passes, Login.tsx exists and exports Login component</verify>
  <done>Login page created with email/password form, company branding, Shadcn/UI styling, and useAuth integration</done>
</task>

<task type="auto">
  <name>Task 2: Add error handling and integrate login route</name>
  <files>src/pages/Login.tsx, src/App.tsx</files>
  <action>
    Add error handling to Login.tsx handleSubmit:
    - Wrap login() call in try/catch
    - On success: navigate to '/chat' (default landing page from App.tsx)
    - On error: Check error.response.status (DISCOVERY.md Section 7.1)
      - 401: "Invalid email or password"
      - 404: "User not found"
      - Other: "An error occurred. Please try again."
    - Display error message above form with red text/background
    - Clear error on new input

    Add login route to App.tsx:
    - Add Route for path="/login" with Login element (NOT protected)
    - Move Navigate redirect from "/" to check auth: if authenticated, go to /chat, else /login
    - Import Login component

    DO NOT add checkpoint - config has skip_checkpoints: true. Build is the verification.
  </action>
  <verify>npm run build succeeds, /login route accessible, error messages display for invalid credentials</verify>
  <done>Error handling implemented with status-specific messages, /login route added to App.tsx, redirect logic updated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] src/pages/Login.tsx exports Login component
- [ ] Login page uses Shadcn/UI Button and Input components
- [ ] Form validates email format (HTML5 validation sufficient)
- [ ] Error messages display for 401/404 errors
- [ ] /login route exists in App.tsx (not protected)
- [ ] Successful login redirects to /chat
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Custom login page matches design requirements (branding, color palette)
- Error handling covers all auth failure cases
- Login route integrated into React Router
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-authentication-system/03-02-SUMMARY.md`:

# Phase 3 Plan 2: Login Page UI Summary

**Custom login interface with authentication and error handling complete**

## Accomplishments

- Created custom login page with company branding
- Implemented email/password form with Shadcn/UI components
- Added comprehensive error handling (401, 404, generic errors)
- Integrated login route into React Router
- Applied color palette (deep-blue background, cyan accents)
- Connected to AuthContext for authentication flow

## Files Created/Modified

- `src/pages/Login.tsx` - Custom login page component
- `src/App.tsx` - Added /login route
- [If needed] `src/components/ui/input.tsx` - Shadcn Input component

## Decisions Made

**Landing page after login:** Redirect to /chat as the default authenticated experience (matches roadmap primary feature).

**Error message strategy:** Status-specific messages for 401/404, generic message for other errors to avoid leaking system information.

**Branding consistency:** Reused Building2 icon + "Company Name" pattern from Sidebar for visual continuity.

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for Plan 03-03: Update ProtectedRoute with real authentication logic using AuthContext.
</output>
