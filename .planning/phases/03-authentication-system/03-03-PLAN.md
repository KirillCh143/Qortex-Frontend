---
phase: 03-authentication-system
plan: 03
type: execute
depends_on: ["03-01"]
files_modified: [src/components/ProtectedRoute.tsx, src/components/Header.tsx]
---

<objective>
Implement real authentication logic in ProtectedRoute and add logout functionality.

Purpose: Complete the authentication system by protecting routes and enabling logout.
Output: ProtectedRoute redirects unauthenticated users to login, Header includes logout button.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-authentication-system/DISCOVERY.md
@.planning/phases/03-authentication-system/03-01-SUMMARY.md
@src/components/ProtectedRoute.tsx
@src/components/Header.tsx
@src/contexts/AuthContext.tsx

**Tech stack available:**
- AuthContext with isAuthenticated, loading, logout methods
- React Router Navigate for redirects
- Shadcn/UI Button component
- Lucide React icons (LogOut for logout button)

**Established patterns:**
- Loading states prevent race conditions (DISCOVERY.md Section 10.5)
- Replace navigation after redirect (prevent back button issues)
- Header already has user avatar display

**Constraining decisions:**
- ProtectedRoute currently returns true stub (src/components/ProtectedRoute.tsx:7)
- Must show loading state before checking auth to prevent race condition

**From Plan 03-01:**
- AuthContext provides: isAuthenticated (boolean), loading (boolean), logout (async function)
- checkAuth runs on mount to validate stored tokens
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ProtectedRoute with real authentication logic</name>
  <files>src/components/ProtectedRoute.tsx</files>
  <action>
    Replace the stub implementation with real authentication logic following DISCOVERY.md Section 6.2 pattern.

    Implementation:
    - Import useAuth from @/contexts/AuthContext
    - Import Navigate from react-router-dom
    - Destructure isAuthenticated and loading from useAuth()
    - If loading is true: return loading UI (simple "Loading..." div or spinner)
    - If not loading and isAuthenticated: return children
    - If not loading and NOT isAuthenticated: return Navigate to="/login" replace

    CRITICAL: Check loading BEFORE checking isAuthenticated (prevents race condition where redirect happens before tokens load from localStorage - DISCOVERY.md Section 10.5 pitfall).

    Use replace prop on Navigate to prevent back button from returning to protected route after logout.

    DO NOT use Outlet - current implementation accepts children prop, keep that pattern for consistency with existing code.
  </action>
  <verify>TypeScript compilation passes, ProtectedRoute redirects to /login when not authenticated</verify>
  <done>ProtectedRoute updated with loading state check, authentication validation, and redirect to login for unauthenticated users</done>
</task>

<task type="auto">
  <name>Task 2: Add logout button to Header component</name>
  <files>src/components/Header.tsx</files>
  <action>
    Add logout functionality to Header component.

    Requirements:
    - Import useAuth, useNavigate
    - Import LogOut icon from lucide-react
    - Add logout button next to user avatar
    - Button onClick handler: call logout() from useAuth, then navigate('/login')
    - Use Shadcn Button with variant="ghost" for consistency with Sidebar buttons
    - Icon: LogOut from lucide-react (size 18, matching other icons)
    - Add hover state (same pattern as Sidebar nav items)

    Error handling: Wrap logout() in try/catch, on error navigate to /login anyway (DISCOVERY.md Section 7.2 - if logout fails, clear client state and redirect).

    Styling: Place logout button after the Avatar, use same spacing pattern as existing header elements.
  </action>
  <verify>npm run build succeeds, Header includes logout button with LogOut icon</verify>
  <done>Logout button added to Header with error handling, icon styling, and navigation to login after logout</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] npm run build succeeds without errors
- [ ] ProtectedRoute shows loading state before checking auth
- [ ] Unauthenticated access to protected routes redirects to /login
- [ ] Header includes logout button with LogOut icon
- [ ] Clicking logout clears session and redirects to /login
- [ ] No TypeScript errors
- [ ] Authentication flow works end-to-end (login → protected routes → logout)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- ProtectedRoute implements real authentication with loading state
- Logout functionality works and clears localStorage
- Phase 3 complete: Authentication system fully functional
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-authentication-system/03-03-SUMMARY.md`:

# Phase 3 Plan 3: Protected Routes Integration Summary

**Authentication system complete with protected routes and logout functionality**

## Accomplishments

- Updated ProtectedRoute with real authentication logic and loading states
- Added logout button to Header with LogOut icon
- Implemented proper redirect flow (unauthenticated → /login)
- Added error handling for logout failures
- Completed end-to-end authentication flow

## Files Created/Modified

- `src/components/ProtectedRoute.tsx` - Real authentication logic with loading state
- `src/components/Header.tsx` - Logout button with navigation

## Decisions Made

**Loading state priority:** Check loading before isAuthenticated to prevent race condition where tokens haven't loaded from localStorage yet (DISCOVERY.md Section 10.5).

**Replace navigation:** Use replace prop on Navigate to prevent back button returning to protected route after logout.

**Logout error handling:** Navigate to /login even if logout API call fails (clear client-side state regardless of server response).

## Issues Encountered

[Problems and resolutions, or "None"]

## Deviations

[If any deviations from plan, or "None"]

## Verification Results

- [x] Authentication flow works end-to-end
- [x] Protected routes redirect unauthenticated users
- [x] Login redirects to /chat after success
- [x] Logout clears session and returns to /login
- [x] Loading states prevent race conditions
- [x] No TypeScript errors

## Next Phase Readiness

**Phase 3 Complete** - Authentication system fully functional with:
- Directus SDK configured for JSON auth mode
- AuthContext managing user state and tokens
- Custom login page with error handling
- Protected routes with loading states
- Logout functionality

**Ready for Phase 4:** Chat Interface - Mock Mode can now assume authenticated user context and build chat UI with mock data.

**Note for Phase 7:** Current implementation uses real Directus authentication. Phase 7 will add mock/real toggle by creating abstraction layer around AuthContext and Directus client.
</output>
