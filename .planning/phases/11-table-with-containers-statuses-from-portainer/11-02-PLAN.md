---
phase: 11-table-with-containers-statuses-from-portainer
plan: 02
type: execute
depends_on: ["11-01"]
files_modified: [src/pages/Settings.tsx]
---

<objective>
Transform Settings page into container monitoring dashboard with status table.

Purpose: Replace Chat Preferences with real-time container monitoring, providing at-a-glance operational visibility for admins/devops users.
Output: Container status table with color-coded indicators, manual refresh, and purple theme styling.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-portainer-containers/11-CONTEXT.md
@.planning/phases/11-table-with-containers-statuses-from-portainer/11-01-SUMMARY.md
@src/pages/Settings.tsx
@src/pages/KnowledgeBase.tsx
@src/components/Sidebar.tsx

**Phase 11 Context:**
- Settings page repurposed for container monitoring
- Remove Chat Preferences Card completely
- Manual refresh with timestamp display
- Color-coded status: green (running), red (stopped), yellow (paused/unhealthy)
- Read-only monitoring (no actions)
- Purple theme (#8466e4, #7049f3) matching Phase 9 design
- Russian localization throughout

**From Plan 11-01:**
- useContainers hook available: { data, isLoading, error, refetch }
- Container type: { id, name, status, uptime, health }
- ContainerStatus: 'running' | 'stopped' | 'paused'
- ContainerHealth: 'healthy' | 'unhealthy' | 'none'

**Design patterns from Phase 9:**
- Purple buttons: bg-[#8466e4] hover:bg-[#7049f3]
- Table styling: light gray headers (bg-gray-50), hover states
- Russian text throughout
- Clean white backgrounds with subtle shadows
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transform Settings page into container monitoring table</name>
  <files>src/pages/Settings.tsx</files>
  <action>
Completely replace Settings.tsx content:

1. Remove all imports related to settings (saveSettings, loadSettings, ApiSettings, Switch, form state)
2. Import useContainers from @/hooks/useContainers
3. Import Table components from @/components/ui/table
4. Import Button from @/components/ui/button
5. Import RefreshCw from lucide-react

Page structure:
- Title: "Контейнеры" (h1, text-3xl font-bold text-gray-900 mb-6)
- Card with table inside
- CardHeader:
  - CardTitle: "Статус контейнеров"
  - CardDescription: "Мониторинг состояния Docker контейнеров в Portainer"

Table structure (use Table, TableHeader, TableBody, TableRow, TableHead, TableCell from ui/table):
- Headers (Russian): Имя | Статус | Время работы | Здоровье
- Table styling: bg-gray-50 for header row, hover:bg-gray-50 for body rows
- Each row displays container data

Status column styling (use flex items-center gap-2):
- Running: Green dot (bg-green-500) + "Запущен" text (text-green-700)
- Stopped: Red dot (bg-red-500) + "Остановлен" text (text-red-700)
- Paused: Yellow dot (bg-yellow-500) + "Приостановлен" text (text-yellow-700)
- Dot: w-2 h-2 rounded-full

Health column styling:
- healthy: "Здоровый" (text-green-700)
- unhealthy: "Нездоровый" (text-red-700)
- none: "—" (text-gray-400)

Loading state: Show "Загрузка..." in center
Error state: Show error message in Card

Keep same page wrapper structure: div with h-full overflow-y-auto and inner div with p-8 max-w-4xl (increase from max-w-2xl for table width).
  </action>
  <verify>Page loads, table displays container data, status indicators show correct colors, Russian text throughout.</verify>
  <done>Settings page shows container table with colored status indicators, proper styling, and Russian localization.</done>
</task>

<task type="auto">
  <name>Task 2: Add manual refresh controls with timestamp</name>
  <files>src/pages/Settings.tsx</files>
  <action>
Add refresh functionality:

1. Import useState and useEffect from react
2. Add state for lastUpdated: useState&lt;Date | null&gt;(null)
3. Get refetch from useContainers() destructuring
4. Create handleRefresh function:
   - Call refetch()
   - Update lastUpdated to new Date()
5. useEffect to set lastUpdated on initial data load (when data becomes available first time)

UI additions:
- Add flex container between CardDescription and Table
- Left side: "Последнее обновление: [formatted timestamp]" (text-sm text-gray-600)
- Right side: Refresh button
- Timestamp format: new Intl.DateTimeFormat('ru-RU', { hour: '2-digit', minute: '2-digit', second: '2-digit' }).format(lastUpdated)
- If no lastUpdated yet, show "—"

Refresh button styling:
- Use Button component
- Purple theme: className="bg-[#8466e4] hover:bg-[#7049f3] text-white"
- Icon: RefreshCw icon (h-4 w-4)
- Text: "Обновить"
- Gap between icon and text: gap-2
- Disable button while isLoading (disabled={isLoading})
- Add loading spinner: RefreshCw with className={isLoading ? 'animate-spin' : ''}

Polish:
- Add mb-4 to the flex container (spacing before table)
- Use justify-between for flex layout
- Align items center (items-center)
</action>
  <verify>Refresh button updates data and timestamp, timestamp shows in Russian format, button disabled during loading with spinner animation.</verify>
  <done>Manual refresh works, timestamp updates correctly in Russian format, purple theme applied, loading states handled properly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Settings page displays container table
- [ ] Status colors correct: green (running), red (stopped), yellow (paused)
- [ ] Health indicators display properly
- [ ] Refresh button updates data and timestamp
- [ ] All text in Russian
- [ ] Purple theme applied to button
- [ ] Loading states work correctly
</verification>

<success_criteria>

- All tasks completed
- Settings page transformed into container monitoring dashboard
- Chat Preferences completely removed
- Table displays all container information with color coding
- Manual refresh with timestamp works
- Purple theme and Russian localization applied
- Phase 11 complete
</success_criteria>

<output>
After completion, create `.planning/phases/11-table-with-containers-statuses-from-portainer/11-02-SUMMARY.md`:

# Phase 11 Plan 2: Container Monitoring UI Summary

**Settings page transformed into Portainer container monitoring dashboard**

## Accomplishments

- Completely replaced Settings page content
- Removed Chat Preferences section
- Implemented container status table with color-coded indicators
- Added manual refresh button with timestamp
- Applied purple theme and Russian localization
- Phase 11 complete

## Files Created/Modified

- `src/pages/Settings.tsx` - Transformed into container monitoring dashboard

## Decisions Made

- Table layout over cards for better data density
- Status colors: green (running), red (stopped), yellow (paused)
- Russian timestamp format with Intl.DateTimeFormat
- Purple button theme consistent with Phase 9
- Manual-only refresh (no auto-refresh to avoid unnecessary API calls)

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

Phase 11 complete. Settings page now serves as container monitoring dashboard. System visibility established for ops/admin users.

Ready for next phase or additional features as needed.
</output>
