---
phase: 12-small-improvements-and-fixes
plan: 04
type: execute
depends_on: []
files_modified: [src/services/n8n/types.ts, src/services/n8n/webhook.service.ts, src/pages/Chat.tsx]
---

<objective>
Add user identity tracking to chat webhook payload with FirstName_LastName session key.

Purpose: Enable proper user tracking and analytics in n8n workflow by including user identification in webhook calls.
Output: Chat webhook includes "sessionidkey" field formatted as FirstName_LastName from Directus user data.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-small-improvements-and-fixes/12-CONTEXT.md
@src/services/n8n/types.ts
@src/services/n8n/webhook.service.ts
@src/pages/Chat.tsx
@src/contexts/AuthContext.tsx

**Established patterns:**
- Zod schemas for type validation (ChatQueryPayloadSchema, WebhookResponseSchema)
- User data available via useAuth hook with user.first_name and user.last_name
- Webhook payload includes question, mode, sessionId, history

**Constraining decisions:**
- Phase 7: Webhook service validates payloads with Zod schemas
- Phase 7.1: User-specific chat history stored in Directus
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update webhook types to include sessionidkey</name>
  <files>src/services/n8n/types.ts</files>
  <action>Add sessionidkey field to ChatQueryPayload interface and ChatQueryPayloadSchema Zod schema. Type: z.string(). Position after sessionId field. Update WebhookResponse if needed (likely unchanged). Ensure schema validates new required field.</action>
  <verify>TypeScript compiles, Zod schema validation passes with sessionidkey field</verify>
  <done>ChatQueryPayload includes sessionidkey: string field, schema validates correctly</done>
</task>

<task type="auto">
  <name>Task 2: Add sessionidkey to webhook calls in Chat page</name>
  <files>src/pages/Chat.tsx</files>
  <action>Update chatMutation.mutate call in handleSend function. Add sessionidkey field formatted as `${user.first_name}_${user.last_name}`. Handle edge cases: if either name is undefined/null, use fallback "Unknown_User". Trim whitespace from names. Example: user { first_name: "Иван", last_name: "Петров" } produces sessionidkey: "Иван_Петров". Preserve existing fields (question, mode, sessionId, history). Verify type safety with updated ChatQueryPayload interface.</action>
  <verify>npm run dev, send chat message, check browser DevTools Network tab, verify POST to webhook includes sessionidkey with FirstName_LastName format</verify>
  <done>Webhook payload includes sessionidkey formatted as FirstName_LastName, fallback handles missing names</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run dev runs without errors
- [ ] Webhook payload includes sessionidkey field
- [ ] SessionIdKey formatted correctly as FirstName_LastName
- [ ] No TypeScript errors
- [ ] Zod validation passes for updated payload
</verification>

<success_criteria>

- ChatQueryPayload type updated with sessionidkey field
- Chat.tsx sends sessionidkey in webhook calls
- All verification checks pass
- User identity tracking enabled for n8n analytics
</success_criteria>

<output>
After completion, create `.planning/phases/12-small-improvements-and-fixes/12-04-SUMMARY.md`
</output>
