---
phase: 12-small-improvements-and-fixes
plan: 04
type: execute
depends_on: []
files_modified: [src/services/n8n/types.ts, src/services/n8n/webhook.service.ts, src/pages/Chat.tsx, src/hooks/useChatQuery.ts]
---

<objective>
Add sessionidkey field to webhook payload with FirstName_LastName format.

Purpose: Enable proper user tracking and analytics in n8n workflow.
Output: Webhook payload includes sessionidkey derived from Directus user data.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-small-improvements-and-fixes/12-CONTEXT.md
@src/services/n8n/types.ts
@src/services/n8n/webhook.service.ts
@src/pages/Chat.tsx
@src/hooks/useChatQuery.ts
@src/contexts/AuthContext.tsx

**Tech stack available:** Zod for schema validation, TypeScript, Directus user data includes first_name and last_name
**Established patterns:**
- Phase 7: Webhook payload validated with Zod schemas before sending
- Phase 7.1: User data available via useAuth hook includes full user object from Directus

**Constraining decisions:**
- All webhook payloads must pass ChatQueryPayloadSchema validation
- User data format: { id, first_name, last_name, email, ... }
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sessionidkey to webhook payload</name>
  <files>src/services/n8n/types.ts, src/services/n8n/webhook.service.ts, src/pages/Chat.tsx, src/hooks/useChatQuery.ts</files>
  <action>Update ChatQueryPayloadSchema in types.ts to include sessionidkey: z.string(). Update ChatQueryPayload type inference. In Chat.tsx handleSend function: construct sessionidkey from user data as `${user.first_name}_${user.last_name}` (fallback to `User_${user.id}` if names missing). Pass sessionidkey in chatMutation.mutate payload alongside existing question, mode, sessionId, history fields. Update useChatQuery hook signature if needed to accept sessionidkey. Webhook service will automatically validate new field via updated schema. Mock webhook service (src/services/mock/webhook.mock.ts) should accept but ignore sessionidkey (log it for debugging). Maintain backward compatibility: if sessionidkey somehow fails to generate, use fallback value "Unknown_User" rather than crashing.</action>
  <verify>Send chat message, inspect browser network tab to confirm webhook request includes sessionidkey field with FirstName_LastName format. Check both real and mock webhook services accept the payload.</verify>
  <done>Webhook payload includes sessionidkey field, formatted as FirstName_LastName from Directus user data, validated by Zod schema, sent to n8n successfully.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Webhook payload includes sessionidkey field
- [ ] sessionidkey format is FirstName_LastName
- [ ] Zod validation passes with new field
- [ ] Mock and real webhook services handle new field
- [ ] No breaking changes to existing chat functionality
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- sessionidkey included in webhook payload
- Proper format (FirstName_LastName) from Directus
- Backward compatible with fallback handling
</success_criteria>

<output>
After completion, create `.planning/phases/12-small-improvements-and-fixes/12-04-SUMMARY.md`:

# Phase 12 Plan 4: Webhook Session Key Summary

**[Substantive one-liner - what shipped, not "plan complete"]**

## Accomplishments

- [Key outcome]

## Files Created/Modified

- `src/services/n8n/types.ts` - Description
- `src/pages/Chat.tsx` - Description
- Other files modified

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 12-05-PLAN.md (Streaming Chat Responses)
</output>
