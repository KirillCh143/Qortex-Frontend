---
phase: 12-small-improvements-and-fixes
plan: 05
type: execute
depends_on: []
files_modified: [src/services/n8n/webhook.service.ts, src/hooks/useChatQuery.ts, src/pages/Chat.tsx, src/services/mock/webhook.mock.ts]
---

<objective>
Enable ChatGPT-style streaming responses with word-by-word reveal.

Purpose: Modern chat UX that displays responses as they generate, matching user expectations from ChatGPT.
Output: Working SSE-based streaming chat responses with smooth token-by-token display.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-small-improvements-and-fixes/12-CONTEXT.md
@USER_IMPUT_FILES/RAG_Workflow_AIAgent.json
@src/services/n8n/webhook.service.ts
@src/hooks/useChatQuery.ts
@src/pages/Chat.tsx

**Tech stack available:** Fetch API with ReadableStream, React state management, n8n webhook with enableStreaming: true
**Established patterns:**
- Phase 7: Webhook service uses fetch with JSON response
- Phase 4: Messages stored as complete strings in Directus

**Context from research:**
- n8n workflow already has enableStreaming: true in "Respond to Frontend Webhook" node
- Need to consume Server-Sent Events or chunked transfer encoding from n8n
- Frontend should display partial responses as they arrive, token by token

**Implementation approach:**
- Replace response.json() with ReadableStream processing
- Parse SSE format (data: lines) or chunked JSON
- Update UI state incrementally as chunks arrive
- Save only complete response to Directus after stream ends
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update webhook service to handle streaming responses</name>
  <files>src/services/n8n/webhook.service.ts, src/services/mock/webhook.mock.ts</files>
  <action>Modify sendQuery function to detect and handle streaming responses. After fetch completes: check Content-Type header for 'text/event-stream' or check if body is readable stream. If streaming: read response.body.getReader(), process chunks in loop (await reader.read()), decode with TextDecoder, parse SSE format (split on \n, extract data: prefix), accumulate partial answer, yield/callback each chunk to caller. Return final accumulated answer. If not streaming: fall back to existing response.json() logic. Update return type to support streaming callback: modify sendQuery signature to accept optional onChunk?: (text: string) => void callback. In mock service: simulate streaming by splitting mock answer into words, yielding each word with 50ms delay using setTimeout in async loop. Keep existing timeout and error handling logic.</action>
  <verify>Check network tab shows streaming response, console.log each chunk as it arrives, verify complete response matches non-streaming behavior.</verify>
  <done>Webhook service processes streaming responses, invokes onChunk callback for each token, returns complete response at end, falls back to JSON for non-streaming endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Update Chat UI to display streaming responses</name>
  <files>src/pages/Chat.tsx, src/hooks/useChatQuery.ts</files>
  <action>Add state for streaming response: streamingResponse (string), isStreaming (boolean). When user sends message and assistant starts responding: create temporary assistant message with empty content, set isStreaming=true. Modify chatMutation.mutate to pass onChunk callback that updates streamingResponse state with accumulated text. Render streaming message using MessageBubble with streamingResponse content while isStreaming is true. After stream completes (onSuccess): save final response to Directus, clear streamingResponse, set isStreaming=false. The temporary message should be replaced by persisted message from Directus. Handle errors mid-stream: if stream fails, save partial response with error indicator. Keep typing indicator from Plan 01 - it shows before streaming starts. Remove typing indicator when first chunk arrives and streaming message appears. Ensure smooth UX: no flashing, seamless transition from typing indicator → streaming → persisted message.</action>
  <verify>Send chat message, confirm response appears word-by-word in real-time, typing indicator shows before streaming starts, final message persists after stream completes.</verify>
  <done>Chat UI displays streaming responses token-by-token, smooth transition from typing indicator to streaming to persisted message, error handling for incomplete streams.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Streaming chat responses with word-by-word reveal</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Navigate to /chat
    3. Send a test message in RAG or LLM mode
    4. Observe typing indicator appears immediately
    5. Observe response begins streaming word-by-word (should feel like ChatGPT)
    6. Confirm complete response persists after streaming finishes
    7. Test error case: disable n8n workflow, send message, verify error handling
    8. Check both RAG and LLM modes work with streaming
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Streaming responses display word-by-word
- [ ] Complete responses persist to Directus
- [ ] Typing indicator shows before stream starts
- [ ] Error handling works for failed streams
- [ ] Both RAG and LLM modes support streaming
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Streaming responses work end-to-end
- UX feels smooth and ChatGPT-like
- No data loss or incomplete messages
- User approval obtained via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/12-small-improvements-and-fixes/12-05-SUMMARY.md`:

# Phase 12 Plan 5: Streaming Chat Responses Summary

**[Substantive one-liner - what shipped, not "plan complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/services/n8n/webhook.service.ts` - Description
- `src/hooks/useChatQuery.ts` - Description
- `src/pages/Chat.tsx` - Description
- `src/services/mock/webhook.mock.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 12 complete - all improvements and fixes delivered
</output>
