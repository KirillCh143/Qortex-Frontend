---
phase: 12-small-improvements-and-fixes
plan: 05
type: execute
depends_on: []
files_modified: [USER_IMPUT_FILES/RAG_Workflow_AIAgent.json, src/services/n8n/webhook.service.ts, src/pages/Chat.tsx]
---

<objective>
Enable ChatGPT-style streaming responses with word-by-word reveal in chat interface.

Purpose: Modern chat UX matching user expectations from ChatGPT, providing smooth token-by-token response streaming.
Output: Chat responses stream word-by-word using Server-Sent Events, n8n workflow configured for streaming output.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-small-improvements-and-fixes/12-CONTEXT.md
@USER_IMPUT_FILES/RAG_Workflow_AIAgent.json
@src/services/n8n/webhook.service.ts
@src/pages/Chat.tsx

**Technical context:**
- n8n workflow at USER_IMPUT_FILES/RAG_Workflow_AIAgent.json already has "Respond to Frontend Webhook" node
- Current node likely needs enableStreaming: true configuration
- Frontend currently uses fetch with response.json() - needs SSE handling
- React state updates required for incremental message display

**Established patterns:**
- Webhook service in services/n8n/webhook.service.ts
- Chat messages saved to Directus after complete
- React Query mutations for async operations
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Update n8n workflow to enable streaming responses</action>
  <instructions>
    I've analyzed USER_IMPUT_FILES/RAG_Workflow_AIAgent.json and identified the "Respond to Frontend Webhook" node.

    To enable streaming:
    1. Open n8n workflow editor (typically http://localhost:5678)
    2. Find "RAG Workflow (AI Agent)" workflow
    3. Locate "Respond to Frontend Webhook" node
    4. In node settings, enable streaming output:
       - Look for "Response Mode" or "Streaming" option
       - Enable "Stream Response" or set responseMode: "stream"
       - Ensure Content-Type set to "text/event-stream" for SSE
    5. Save workflow
    6. Test webhook endpoint still responds (may need workflow restart)

    The workflow JSON shows the webhook node exists - we need to enable its streaming capability in n8n UI.
  </instructions>
  <verification>Webhook endpoint responds with Content-Type: text/event-stream header, sends data in SSE format (data: {...}\n\n)</verification>
  <resume-signal>Type "done" when n8n workflow updated and streaming enabled</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Implement SSE handling in webhook service</name>
  <files>src/services/n8n/webhook.service.ts</files>
  <action>Add streaming support to webhook service. Create new method sendQueryStream that returns AsyncGenerator<string>. Use fetch with ReadableStream instead of response.json(). Read stream with reader.read() in async loop. Parse SSE format: lines starting with "data: " contain JSON chunks. Yield content incrementally. Handle [DONE] signal to close stream. Keep existing sendQuery for backward compatibility. Example: async *sendQueryStream(payload) { const response = await fetch(...); const reader = response.body.getReader(); while (true) { const {done, value} = await reader.read(); if (done) break; const text = new TextDecoder().decode(value); const lines = text.split('\n'); for (const line of lines) { if (line.startsWith('data: ')) { const data = JSON.parse(line.slice(6)); yield data.content; } } } }</action>
  <verify>TypeScript compiles, AsyncGenerator type correct, SSE parsing logic handles chunks properly</verify>
  <done>Webhook service includes sendQueryStream method returning AsyncGenerator, SSE parsing implemented</done>
</task>

<task type="auto">
  <name>Task 2: Update Chat page to consume streaming responses</name>
  <files>src/pages/Chat.tsx</files>
  <action>Modify handleSend to use streaming. Add state for streaming message: const [streamingContent, setStreamingContent] = useState(''). Instead of chatMutation.mutate, call webhookService.sendQueryStream directly. Iterate with for await (const chunk of stream) { setStreamingContent(prev => prev + chunk) }. Display streamingContent as temporary assistant message bubble while streaming. On complete, save full content to Directus with saveMutation. Clear streamingContent after save. Show typing indicator before first chunk arrives. Handle errors by falling back to non-streaming mode. Keep messages array displaying saved messages, streaming message overlays at bottom during active stream.</action>
  <verify>npm run dev, send chat message, verify response appears word-by-word with smooth reveal, saves to history after complete</verify>
  <done>Chat streams responses word-by-word, saves complete message after streaming finishes, fallback to non-streaming on error</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run dev runs without errors
- [ ] n8n workflow streams responses (verified manually)
- [ ] Chat displays streaming responses word-by-word
- [ ] Complete messages save to Directus after streaming
- [ ] No TypeScript errors
- [ ] Streaming feels smooth like ChatGPT
</verification>

<success_criteria>

- n8n workflow configured for streaming (checkpoint verified)
- Webhook service handles SSE with AsyncGenerator
- Chat page consumes stream and displays incrementally
- All verification checks pass
- ChatGPT-style streaming experience achieved
</success_criteria>

<output>
After completion, create `.planning/phases/12-small-improvements-and-fixes/12-05-SUMMARY.md`
</output>
