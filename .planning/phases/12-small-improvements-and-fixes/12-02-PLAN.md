---
phase: 12-small-improvements-and-fixes
plan: 02
type: execute
depends_on: []
files_modified: [src/components/FileDetailPanel.tsx, src/pages/KnowledgeBase.tsx]
---

<objective>
Extract File Detail Panel into standalone component for better code organization and maintainability.

Purpose: Prepare Knowledge Base for upcoming edit/move features by isolating detail panel logic into reusable component.
Output: FileDetailPanel.tsx component extracted, KnowledgeBase.tsx simplified and cleaner.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-small-improvements-and-fixes/12-CONTEXT.md
@src/pages/KnowledgeBase.tsx

**Established patterns:**
- Component props use TypeScript interfaces
- Shadcn/UI Dialog components for modals
- File type icons with colored backgrounds from Phase 9
- Russian localization throughout

**Constraining decisions:**
- Phase 9: File type icons (PDF red, DOCX blue, XLSX green)
- Phase 7.3: Detail panel with download-only (no view button)
- Phase 9: Purple theme and modern polish
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FileDetailPanel component</name>
  <files>src/components/FileDetailPanel.tsx</files>
  <action>Create new component FileDetailPanel.tsx. Extract the entire Detail Panel JSX from KnowledgeBase.tsx (lines ~300-450, the section with Dialog wrapping file details). Props: { file: DirectusFile | null, isOpen: boolean, onClose: () => void, onDownload: (file: DirectusFile) => void, isDownloading: boolean }. Move getFileTypeInfo helper function into this component. Keep all existing styling (rounded-2xl, purple accents, file type icons with colors). Include all imports: Dialog components, Lucide icons (X, Download, Loader2, User, Calendar, FileText, FileType, Sheet, File), formatFileSize, formatDateRussian, cn utility. Maintain exact visual appearance - this is pure refactoring.</action>
  <verify>TypeScript compiles without errors, component exports correct interface</verify>
  <done>FileDetailPanel.tsx created with props interface, helper function, and complete panel JSX extracted</done>
</task>

<task type="auto">
  <name>Task 2: Update KnowledgeBase to use FileDetailPanel</name>
  <files>src/pages/KnowledgeBase.tsx</files>
  <action>Replace inline Detail Panel JSX in KnowledgeBase.tsx with FileDetailPanel component import. Remove getFileTypeInfo helper (now in FileDetailPanel). Pass props: file={selectedDoc}, isOpen={isPanelOpen}, onClose={handleClosePanel}, onDownload={(file) => downloadMutation.mutate(file.id)}, isDownloading={downloadMutation.isPending && downloadMutation.variables === selectedDoc?.id}. Keep all state management (selectedDoc, isPanelOpen) in parent. Verify no functionality changes - purely organizational refactoring.</action>
  <verify>npm run dev, navigate to Knowledge Base, click file, verify detail panel opens/closes correctly, download button works, no visual changes</verify>
  <done>KnowledgeBase.tsx uses FileDetailPanel component, detail panel functionality unchanged, code cleaner and more maintainable</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run dev runs without errors
- [ ] Detail panel opens/closes correctly
- [ ] Download button functionality preserved
- [ ] Visual appearance unchanged
- [ ] No TypeScript errors
</verification>

<success_criteria>

- FileDetailPanel component created and exported
- KnowledgeBase.tsx refactored to use new component
- All verification checks pass
- Zero functional or visual changes (pure refactoring)
</success_criteria>

<output>
After completion, create `.planning/phases/12-small-improvements-and-fixes/12-02-SUMMARY.md`
</output>
