---
phase: 12-small-improvements-and-fixes
plan: 03
type: execute
depends_on: ["12-02"]
files_modified: [src/components/FileDetailPanel.tsx, src/hooks/useFiles.ts]
---

<objective>
Add file editing and folder relocation features to Knowledge Base Detail Panel.

Purpose: Enable users to manage files directly from detail view without navigating away.
Output: Users can edit file names/descriptions and move files to different folders from FileDetailPanel.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-small-improvements-and-fixes/12-CONTEXT.md
@.planning/phases/12-small-improvements-and-fixes/12-02-SUMMARY.md
@src/components/FileDetailPanel.tsx
@src/hooks/useFiles.ts
@src/services/directus/files.service.ts

**Established patterns:**
- React Query mutations for async operations (useUpdateFile, useMoveFile)
- Directus PATCH endpoint for file metadata updates
- Russian labels for forms ("Название", "Описание", "Папка")
- Purple theme (#8466e4) for primary actions

**Constraining decisions:**
- Phase 8 UAT: Two-step file operations (POST binary, PATCH metadata)
- Phase 9: Purple buttons with Russian text
- Phase 7.3: Folder hierarchy with parent/child relationships
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add file edit functionality to Detail Panel</name>
  <files>src/components/FileDetailPanel.tsx, src/hooks/useFiles.ts</files>
  <action>Add edit mode to FileDetailPanel. Create useUpdateFile hook in useFiles.ts that calls directusService.updateFile with PATCH /files/{id} passing { title, description }. In FileDetailPanel: Add "Редактировать" button (purple, Lucide Edit icon) below download button. On click, toggle edit mode showing Input for title and Textarea for description (auto-resize with min-h-20 max-h-40). Replace static text with editable fields. Add "Сохранить" and "Отмена" buttons in edit mode. On save, call updateFileMutation.mutate({ id, updates: { title, description } }), show Loader2 while pending, exit edit mode on success. Keep purple theme and rounded-2xl styling.</action>
  <verify>npm run dev, open file detail, click "Редактировать", modify title/description, click "Сохранить", verify changes persist after panel close/reopen</verify>
  <done>File edit functionality working, title and description editable inline, changes saved to Directus</done>
</task>

<task type="auto">
  <name>Task 2: Implement move to folder feature</name>
  <files>src/components/FileDetailPanel.tsx, src/hooks/useFiles.ts</files>
  <action>Add move functionality to FileDetailPanel. Create useMoveFile hook in useFiles.ts calling directusService.updateFile with PATCH /files/{id} passing { folder }. In FileDetailPanel: Add "Переместить" button (purple outline, Lucide FolderInput icon) in header next to close X. On click, open inline dropdown or small Dialog showing folder tree (fetch with useFolders hook from existing pattern). Include "Корневая папка" option (folder: null) at top. On folder select, call moveFileMutation.mutate({ id, folderId }), show confirmation toast ("Файл перемещён"), close dialog. Update current folder display in panel immediately. Match Phase 9 dialog styling (purple accents, Russian labels).</action>
  <verify>npm run dev, open file detail, click "Переместить", select different folder, verify file disappears from current folder view and appears in target folder</verify>
  <done>Move to folder working, file relocates correctly, folder tree selector functional with Russian labels</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run dev runs without errors
- [ ] Edit mode toggles correctly, saves changes
- [ ] Move to folder relocates files properly
- [ ] No TypeScript errors
- [ ] Purple theme maintained, Russian localization correct
</verification>

<success_criteria>

- File edit functionality complete with inline editing
- Move to folder feature working with folder tree selector
- All verification checks pass
- Changes persist in Directus
</success_criteria>

<output>
After completion, create `.planning/phases/12-small-improvements-and-fixes/12-03-SUMMARY.md`
</output>
