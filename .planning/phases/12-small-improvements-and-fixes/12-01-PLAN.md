---
phase: 12-small-improvements-and-fixes
plan: 01
type: execute
depends_on: []
files_modified: [src/components/ChatInput.tsx, src/pages/Chat.tsx, src/pages/KnowledgeBase.tsx]
---

<objective>
Polish chat and knowledge base UI with typing indicator and scoped download loading states.

Purpose: Improve user experience with professional visual feedback during async operations.
Output: Chat shows typing indicator while bot responds, Knowledge Base download buttons show loading state only for the file being downloaded.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-small-improvements-and-fixes/12-CONTEXT.md
@src/pages/Chat.tsx
@src/components/ChatInput.tsx
@src/pages/KnowledgeBase.tsx

**Established patterns:**
- Purple theme (#8466e4) and Russian localization from Phase 9
- React Query mutations with isPending state for loading indicators
- Lucide icons for UI elements

**Constraining decisions:**
- Phase 9: Purple brand color, circular send button, modern polish
- Phase 11: Real/mock service toggle pattern with React Query hooks
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typing indicator to chat interface</name>
  <files>src/pages/Chat.tsx, src/components/ChatInput.tsx</files>
  <action>Add typing indicator animation that displays after user sends message and before assistant response arrives. Use Lucide Loader2 icon with spin animation. Display below last user message with purple accent color (#8466e4). Show only when chatMutation.isPending is true. Use Russian text "Печатает..." with animated dots. Match MessageBubble styling (rounded-2xl, padding, shadow). Hide when response arrives.</action>
  <verify>Start dev server (npm run dev), send chat message, confirm typing indicator appears with spinning icon and "Печатает..." text, disappears when response arrives</verify>
  <done>Typing indicator shows during bot response loading, disappears when complete, matches Phase 9 purple theme</done>
</task>

<task type="auto">
  <name>Task 2: Fix download loading bug in Knowledge Base</name>
  <files>src/pages/KnowledgeBase.tsx</files>
  <action>Scope download loading state to individual file IDs instead of global state. Currently downloadMutation.isPending triggers spinners on ALL files. Fix: Track downloadMutation.variables?.id in loading condition - only show Loader2 when downloadMutation.isPending && downloadMutation.variables?.id === file.id. Apply to both grid view (Card download button) and list view (FileListView download column). Keep purple color for Loader2 (#8466e4).</action>
  <verify>npm run dev, navigate to Knowledge Base, click download on one file, verify only that file's button shows loading spinner, other files remain unaffected</verify>
  <done>Download button loading state scoped per file, clicking one file doesn't trigger spinners on others</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run dev runs without errors
- [ ] Chat typing indicator appears/disappears correctly
- [ ] Knowledge Base download loading scoped per file
- [ ] No TypeScript errors
- [ ] Purple theme maintained throughout
</verification>

<success_criteria>

- Typing indicator implemented with Russian text and purple theme
- Download loading bug fixed with per-file scoping
- All verification checks pass
- No visual regressions
</success_criteria>

<output>
After completion, create `.planning/phases/12-small-improvements-and-fixes/12-01-SUMMARY.md`
</output>
