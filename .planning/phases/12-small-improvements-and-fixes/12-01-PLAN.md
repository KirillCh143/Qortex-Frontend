---
phase: 12-small-improvements-and-fixes
plan: 01
type: execute
depends_on: []
files_modified: [src/lib/settings.ts, .env, src/contexts/AuthContext.tsx]
---

<objective>
Migrate message persistence setting from per-browser localStorage to system-level configuration.

Purpose: Ensure all users share the same message persistence behavior configured at system level, not per browser.
Output: Settings read from environment variables instead of localStorage, consistent across all clients.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/settings.ts
@.env

**Current behavior:**
- `messagePersistence` setting stored in localStorage (per browser)
- Each user/browser can have different persistence behavior
- Setting managed via `src/lib/settings.ts`

**Required behavior (from USER_IMPUT_FILES/TO_DO.md #3):**
- Settings must be configured once at system level
- Apply to all users uniformly
- Not stored per browser

**Established patterns:**
- Environment variables used for system-wide config (VITE_API_BASE_URL, VITE_USE_MOCK_DATA)
- .env file for configuration management
- Settings accessed via simple utility functions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add system-level message persistence configuration to .env</name>
  <files>.env</files>
  <action>
Add new environment variable to .env file:
- `VITE_MESSAGE_PERSISTENCE=true` (default: true to match current default behavior)
- Add comment explaining this controls whether chat messages persist across sessions for ALL users
- Follow existing .env pattern with VITE_ prefix for client-side variables
  </action>
  <verify>grep "VITE_MESSAGE_PERSISTENCE" .env shows the new variable</verify>
  <done>.env contains VITE_MESSAGE_PERSISTENCE with true/false value</done>
</task>

<task type="auto">
  <name>Task 2: Update settings.ts to read from environment variable instead of localStorage</name>
  <files>src/lib/settings.ts</files>
  <action>
Refactor src/lib/settings.ts:
1. Update `loadSettings()` function to read from `import.meta.env.VITE_MESSAGE_PERSISTENCE` instead of localStorage
2. Parse the env variable as boolean (handle "true"/"false" strings)
3. Default to true if env variable is not set or invalid
4. REMOVE `saveSettings()` function entirely (no longer needed - settings are read-only from env)
5. Update JSDoc comments to reflect new behavior (system-level, read from env)
6. Keep the ApiSettings interface unchanged for backward compatibility

Avoid localStorage entirely - this ensures uniform behavior across all users/browsers. Setting is now deployment-time configuration, not runtime user preference.
  </action>
  <verify>
1. grep "localStorage" src/lib/settings.ts returns nothing (all localStorage usage removed)
2. grep "import.meta.env.VITE_MESSAGE_PERSISTENCE" src/lib/settings.ts shows usage
3. npm run build completes without TypeScript errors
  </verify>
  <done>
- settings.ts reads messagePersistence from VITE_MESSAGE_PERSISTENCE env variable
- saveSettings function removed
- No localStorage usage remains in settings.ts
- Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove settings save calls from codebase</name>
  <files>src/contexts/AuthContext.tsx</files>
  <action>
Search for and remove any calls to `saveSettings()`:
1. Grep codebase for "saveSettings" imports and calls
2. Remove the import from any files using it
3. Remove the function calls (settings are now read-only from env)
4. If there was UI for toggling persistence, remove it or make it read-only display

The messagePersistence setting is now a deployment configuration, not a user preference. Users can no longer change it - it's set system-wide via .env.
  </action>
  <verify>
1. grep -r "saveSettings" src/ returns no results
2. npm run build completes without errors
3. TypeScript has no unused import warnings
  </verify>
  <done>
- All saveSettings imports removed
- All saveSettings calls removed
- No TypeScript errors
- Build succeeds
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors or warnings
- [ ] `grep "localStorage" src/lib/settings.ts` returns nothing
- [ ] `grep "saveSettings" src/ -r` returns nothing
- [ ] .env contains VITE_MESSAGE_PERSISTENCE variable
- [ ] Message persistence behavior is consistent across all browsers (read from env)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No localStorage usage in settings.ts
- Message persistence is system-level configuration via .env
- No TypeScript errors or build warnings
- Phase 12 complete
</success_criteria>

<output>
After completion, create `.planning/phases/12-small-improvements-and-fixes/12-01-SUMMARY.md`:

# Phase 12 Plan 1: System-Level Settings Migration Summary

**Migrated message persistence from per-browser localStorage to system-wide environment variable**

## Accomplishments

- Added VITE_MESSAGE_PERSISTENCE to .env for system-level configuration
- Refactored settings.ts to read from environment instead of localStorage
- Removed saveSettings function and all localStorage usage
- Removed all saveSettings calls from codebase
- Message persistence now uniform across all users

## Files Created/Modified

- `.env` - Added VITE_MESSAGE_PERSISTENCE variable
- `src/lib/settings.ts` - Refactored to read from env, removed localStorage usage
- `src/contexts/AuthContext.tsx` - Removed saveSettings calls (if any)

## Decisions Made

- Message persistence is now deployment-time configuration (not runtime user preference)
- Default value remains true (matching previous default behavior)
- Setting is read-only from user perspective (configured by admin via .env)

## Issues Encountered

[Document any issues encountered during implementation, or "None"]

## Next Phase Readiness

Phase 12 complete. All items from USER_IMPUT_FILES/TO_DO.md that are frontend-related have been addressed:
- ✅ Item #1: Login redirect (completed in Phase 7.5)
- ❌ Item #2: Redis lock testing (backend concern, out of frontend scope)
- ✅ Item #3: System-level settings (completed in Phase 12)
</output>
