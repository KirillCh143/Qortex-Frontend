---
phase: 12-small-improvements-and-fixes
plan: 01
type: execute
depends_on: []
files_modified: [src/pages/Chat.tsx, src/pages/KnowledgeBase.tsx]
---

<objective>
Polish UI/UX with typing indicator and fix download loading state bug.

Purpose: Improve user feedback during bot responses and fix visual bug in Knowledge Base downloads.
Output: Typing indicator in chat, properly scoped download loading states.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-small-improvements-and-fixes/12-CONTEXT.md
@src/pages/Chat.tsx
@src/pages/KnowledgeBase.tsx
@src/hooks/useChatQuery.ts

**Tech stack available:** React, TypeScript, Tailwind CSS, Lucide icons, React Query
**Established patterns:** Purple theme (#8466e4), Russian localization, loading states with Loader2 icon
**Constraining decisions:**
- Phase 9: Purple brand color (#8466e4) for all primary actions and states
- Phase 11: Loading states should be scoped to specific items, not global

**Issues being addressed:**
- Typing indicator missing - users don't know bot is thinking
- Download button loading state affects all files in list instead of just the clicked file
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typing indicator for bot responses</name>
  <files>src/pages/Chat.tsx</files>
  <action>Add a typing indicator that shows while chatMutation.isPending is true (after user message sent, before assistant response arrives). Display between messages using MessageBubble-like styling: left-aligned bubble with Bot icon avatar, gray background, containing three animated dots. Use Lucide's Loader2 icon with spin animation or create CSS dot animation (.typing-dot with staggered animation-delay). Position above ChatInput overlay, visible in messages scroll area. Should automatically disappear when assistant response arrives.</action>
  <verify>Start dev server, send chat message, confirm typing indicator appears immediately after sending and disappears when response arrives. Indicator should be styled consistently with assistant message bubbles (left-aligned, gray background, bot icon).</verify>
  <done>Typing indicator displays while chatMutation.isPending is true, uses consistent styling with chat bubbles, automatically removes when response completes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix download loading state scoping</name>
  <files>src/pages/KnowledgeBase.tsx</files>
  <action>Current bug: downloadMutation.isPending is global, so clicking any file's download button shows spinner on ALL files. Fix by storing the specific file ID being downloaded in local state (downloadingFileId: string | null). Set it when download starts (downloadingFileId = file.id), clear it when mutation completes (onSuccess/onError). Update Download button condition from isPending to isPending && downloadingFileId === file.id. This scopes the loading spinner to only the file being downloaded. Apply to both Grid View (current inline implementation around line 300) and List View (FileListView component if it also has download buttons).</action>
  <verify>Click download on one file, confirm only that file shows loading spinner, not all files in the list. Test in both grid and list views.</verify>
  <done>Download loading spinner appears only on the specific file being downloaded, not on all files. Works correctly in both grid and list views.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run dev starts without errors
- [ ] Typing indicator appears when sending chat message
- [ ] Typing indicator disappears when response arrives
- [ ] Download button loading state scoped to clicked file only
- [ ] No TypeScript errors introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Typing indicator works in chat interface
- Download loading state bug fixed
- No visual regressions in existing UI
</success_criteria>

<output>
After completion, create `.planning/phases/12-small-improvements-and-fixes/12-01-SUMMARY.md`:

# Phase 12 Plan 1: UI/UX Polish Summary

**[Substantive one-liner - what shipped, not "plan complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/pages/Chat.tsx` - Description
- `src/pages/KnowledgeBase.tsx` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 12-02-PLAN.md (Extract Detail Panel Component)
</output>
