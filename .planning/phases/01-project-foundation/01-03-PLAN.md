---
phase: 01-project-foundation
plan: 03
type: execute
depends_on: ["01-02"]
files_modified: [tailwind.config.js, postcss.config.js, src/index.css, components.json, src/lib/utils.ts, src/components/ui/, package.json]
---

<objective>
Set up Tailwind CSS, Shadcn/UI component library, and establish base folder structure.

Purpose: Configure the UI foundation (Tailwind + Shadcn) and create organized folder structure for components, services, and utilities that all future work will follow.
Output: Tailwind CSS working with custom color palette, Shadcn/UI initialized and ready for component installation, clean folder structure established.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md
@.planning/phases/01-project-foundation/01-02-SUMMARY.md

**Tech stack from PROJECT.md:** Tailwind CSS, Shadcn/UI, Lucide Icons

**Color palette from PROJECT.md:** Deep Blue (primary), Bright Blue (secondary), Cyan (accent) — exact hex values not specified yet, will use reasonable defaults and document for approval in summary.

**Key folders needed:** Components, services/api, hooks, types, lib/utils
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure Tailwind CSS with custom color palette</name>
  <files>package.json, tailwind.config.js, postcss.config.js, src/index.css</files>
  <action>Install Tailwind: `npm install -D tailwindcss postcss autoprefixer`. Run `npx tailwindcss init -p` to create tailwind.config.js and postcss.config.js. Update tailwind.config.js content paths to ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"]. Add custom colors to theme.extend.colors: primary (Deep Blue: #1e3a8a), secondary (Bright Blue: #3b82f6), accent (Cyan: #06b6d4) — reasonable interpretations, document in summary for approval. Replace src/index.css with Tailwind directives: @tailwind base; @tailwind components; @tailwind utilities;. Clear default Vite styles from App.tsx and replace with simple Tailwind test (e.g., bg-primary text-white).</action>
  <verify>npm run dev shows styled component with custom colors, browser devtools shows Tailwind classes applied</verify>
  <done>Tailwind CSS installed, configured with custom color palette, styles rendering correctly</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Shadcn/UI and install utility function</name>
  <files>components.json, src/lib/utils.ts, package.json</files>
  <action>Install Shadcn dependencies: `npm install class-variance-authority clsx tailwind-merge lucide-react`. Run `npx shadcn-ui@latest init` to initialize Shadcn — select: TypeScript: yes, style: default, base color: slate, CSS variables: yes, tailwind.config.js location: default, components location: src/components/ui, utils location: src/lib/utils.ts, React Server Components: no, write to components.json: yes. This creates components.json config and src/lib/utils.ts with cn() utility for class merging.</action>
  <verify>components.json exists, src/lib/utils.ts exports cn function, no errors during init</verify>
  <done>Shadcn/UI initialized with components directory structure and utility functions ready</done>
</task>

<task type="auto">
  <name>Task 3: Create base folder structure and clean up template files</name>
  <files>src/components/, src/services/, src/hooks/, src/types/, src/App.tsx, src/App.css</files>
  <action>Create directory structure: `mkdir -p src/components src/services src/hooks src/types`. Create placeholder files to preserve structure: src/components/.gitkeep, src/services/.gitkeep, src/hooks/.gitkeep, src/types/.gitkeep. Delete unnecessary template files: rm src/App.css (Tailwind handles styling). Update src/App.tsx to minimal component using Tailwind classes (bg-primary, text-white, centered "RAG Agent Frontend" title) to verify styling works. Remove default Vite logo imports and CSS.</action>
  <verify>ls src/ shows components, services, hooks, types directories; npm run dev shows clean styled app with custom colors</verify>
  <done>Folder structure established, template cleanup complete, minimal styled App component rendering</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` shows styled app with custom Tailwind colors
- [ ] `components.json` exists with correct Shadcn configuration
- [ ] Folder structure includes src/components, src/services, src/hooks, src/types
- [ ] src/lib/utils.ts exports cn utility function
- [ ] Tailwind classes work in components
</verification>

<success_criteria>

- All tasks completed
- Tailwind CSS working with custom color palette
- Shadcn/UI initialized and ready for component installation
- Clean folder structure established
- Minimal App component using Tailwind renders successfully
- Phase 1 complete — foundation ready for Phase 2 (layout and navigation)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-03-SUMMARY.md`:

# Phase 1 Plan 3: UI Foundation & Structure Summary

**Tailwind CSS, Shadcn/UI, and folder structure configured**

## Accomplishments

- Installed and configured Tailwind CSS with custom color palette
- Initialized Shadcn/UI component system
- Created organized folder structure (components, services, hooks, types)
- Cleaned up Vite template files
- Built minimal styled App component to verify integration

## Files Created/Modified

- `tailwind.config.js` - Tailwind configuration with custom colors
- `postcss.config.js` - PostCSS configuration for Tailwind
- `src/index.css` - Tailwind directives
- `components.json` - Shadcn/UI configuration
- `src/lib/utils.ts` - Utility functions (cn for class merging)
- `src/components/`, `src/services/`, `src/hooks/`, `src/types/` - Base folder structure
- `src/App.tsx` - Minimal styled component

## Decisions Made

- **Color palette interpretation**: primary: #1e3a8a (Deep Blue), secondary: #3b82f6 (Bright Blue), accent: #06b6d4 (Cyan) — reasonable defaults for PROJECT.md descriptive names, can adjust if needed
- **Shadcn config**: Default style, slate base color, CSS variables enabled
- **Folder structure**: Flat structure under src/ for simplicity (components, services, hooks, types at root level)

## Issues Encountered

None (or describe any)

## Next Phase

Phase 1 complete. Ready for Phase 2: Core Layout & Navigation (sidebar, header, routing).
</output>
