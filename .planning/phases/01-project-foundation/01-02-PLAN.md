---
phase: 01-project-foundation
plan: 02
type: execute
depends_on: ["01-01"]
files_modified: [.eslintrc.cjs, .prettierrc, .vscode/settings.json, package.json]
---

<objective>
Configure code quality tooling with ESLint, Prettier, and VSCode format-on-save.

Purpose: Establish consistent code formatting and linting standards from day one, preventing technical debt and style inconsistencies.
Output: ESLint + Prettier configured and working, VSCode auto-formats on save, all existing code passes linting.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md

**Key constraint from PROJECT.md:** Code quality tooling (Prettier + ESLint + VSCode format-on-save) required from day one.

**Tech stack:** React + TypeScript, so ESLint needs React and TypeScript plugins.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure ESLint with React + TypeScript support</name>
  <files>package.json, .eslintrc.cjs</files>
  <action>Install ESLint with TypeScript and React plugins: `npm install -D eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin eslint-plugin-react eslint-plugin-react-hooks eslint-plugin-react-refresh`. Create .eslintrc.cjs with: parser @typescript-eslint/parser, extends recommended configs (eslint:recommended, plugin:@typescript-eslint/recommended, plugin:react/recommended, plugin:react-hooks/recommended), parserOptions for latest ECMAScript and jsx, settings for React version detection (automatic), rules for react-refresh/only-export-components warn. Avoid deprecated eslint-config-react-app.</action>
  <verify>npx eslint src/ runs without errors, identifies any style issues in existing code</verify>
  <done>ESLint installed and configured, runs successfully on src/ directory</done>
</task>

<task type="auto">
  <name>Task 2: Install and configure Prettier with ESLint integration</name>
  <files>package.json, .prettierrc, .eslintrc.cjs</files>
  <action>Install Prettier and ESLint integration: `npm install -D prettier eslint-config-prettier eslint-plugin-prettier`. Create .prettierrc with: semi: false, singleQuote: true, tabWidth: 2, trailingComma: 'es5', printWidth: 100 (reasonable defaults). Update .eslintrc.cjs extends to include 'plugin:prettier/recommended' at the END (to override conflicting rules). Run `npx prettier --write src/` to format all existing code.</action>
  <verify>npx prettier --check src/ shows no unformatted files, npx eslint src/ passes with Prettier rules active</verify>
  <done>Prettier installed, integrated with ESLint, all code formatted consistently</done>
</task>

<task type="auto">
  <name>Task 3: Configure VSCode settings for format-on-save</name>
  <files>.vscode/settings.json</files>
  <action>Create .vscode/settings.json with: "editor.formatOnSave": true, "editor.defaultFormatter": "esbenp.prettier-vscode", "editor.codeActionsOnSave": {"source.fixAll.eslint": true}, "[typescript]": {"editor.defaultFormatter": "esbenp.prettier-vscode"}, "[typescriptreact]": {"editor.defaultFormatter": "esbenp.prettier-vscode"}. This ensures VSCode uses Prettier for formatting and runs ESLint fixes on save.</action>
  <verify>cat .vscode/settings.json shows correct configuration</verify>
  <done>VSCode settings configured, format-on-save enabled for TypeScript and React files</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx eslint src/` runs without errors
- [ ] `npx prettier --check src/` shows all files formatted
- [ ] `.vscode/settings.json` exists with format-on-save enabled
- [ ] Package.json includes ESLint and Prettier dependencies
</verification>

<success_criteria>

- All tasks completed
- ESLint and Prettier working together without conflicts
- All existing code passes linting and formatting checks
- VSCode configured for automatic formatting
- Ready for Tailwind CSS and Shadcn/UI setup in Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Code Quality Tooling Summary

**ESLint, Prettier, and VSCode format-on-save configured and working**

## Accomplishments

- Installed ESLint with TypeScript and React support
- Configured Prettier with ESLint integration
- Set up VSCode format-on-save for automatic code formatting
- Formatted all existing code to consistent style

## Files Created/Modified

- `.eslintrc.cjs` - ESLint configuration with TypeScript + React rules
- `.prettierrc` - Prettier formatting rules
- `.vscode/settings.json` - VSCode editor settings for format-on-save
- `package.json` - Added ESLint and Prettier dependencies

## Decisions Made

- **Prettier config**: Semi: false, single quotes, 2-space tabs, 100 char line width (industry standard React defaults)
- **ESLint extends order**: Prettier plugin last to override conflicting rules

## Issues Encountered

None (or describe any)

## Next Step

Ready for 01-03-PLAN.md (Tailwind CSS, Shadcn/UI, folder structure)
</output>
