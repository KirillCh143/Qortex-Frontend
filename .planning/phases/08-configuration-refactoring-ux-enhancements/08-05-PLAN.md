---
phase: 08-configuration-refactoring-ux-enhancements
plan: 05
type: execute
depends_on: []
files_modified: [src/pages/KnowledgeBase.tsx, src/hooks/useFolders.ts, src/hooks/useFiles.ts, src/components/CreateFolderDialog.tsx, src/components/UploadFileDialog.tsx]
---

<objective>
Add file management capabilities to Knowledge Base - create folders and upload files.

Purpose: Enable users to organize and contribute to the knowledge base directly from the frontend. Users can create new folders in the hierarchy and upload files to existing folders, eliminating need for backend admin interface.
Output: Knowledge Base with "New Folder" and "Upload File" buttons, functional dialogs for both operations, integration with Directus API.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/KnowledgeBase.tsx
@src/hooks/useFolders.ts
@src/hooks/useFiles.ts
@src/services/directus/types.ts

**Tech stack available:** React, TypeScript, React Query, Directus SDK, Shadcn/UI
**Established patterns:** React Query mutations for data changes, Dialog components from Shadcn/UI
**Constraining decisions:**
- Phase 7.3: Folder hierarchy with FolderTree component, folder-based file filtering
- Phase 7.4: File downloads with Bearer token authentication
- Current: Read-only Knowledge Base (browse, search, download only)

**Current implementation:**
- KnowledgeBase.tsx displays folders (left sidebar) and files (main content)
- useFolders hook fetches folders from Directus
- useFiles hook fetches files with optional folder/search filters
- FolderTree component shows folder hierarchy
- No create/upload functionality exists yet
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create folder creation UI with Directus integration</name>
  <files>src/components/CreateFolderDialog.tsx, src/hooks/useFolders.ts, src/pages/KnowledgeBase.tsx</files>
  <action>
    1. Create CreateFolderDialog.tsx component using Shadcn/UI Dialog:
       - Dialog with "Create Folder" title
       - Input field for folder name (required)
       - Dropdown/Select for parent folder (optional - null means root level)
       - Cancel and Create buttons
       - Create button triggers mutation to create folder via Directus SDK
    2. In useFolders.ts, add useCreateFolder mutation hook:
       - Use React Query useMutation
       - Call Directus SDK: client.request(createFolder({ name, parent }))
       - Invalidate folders query on success to refresh list
       - Handle errors with toast/console.log
    3. In KnowledgeBase.tsx:
       - Add "New Folder" button in folder sidebar header (top of sidebar)
       - Button opens CreateFolderDialog
       - Pass selectedFolderId as default parent (or null if All Documents selected)
       - Use Plus icon from lucide-react

    DO NOT hardcode folder IDs - use selectedFolderId state for parent folder.
    DO NOT add complex validation - Directus handles name uniqueness and constraints.
  </action>
  <verify>npm run build succeeds, New Folder button appears in sidebar, clicking opens dialog</verify>
  <done>CreateFolderDialog component created, useCreateFolder hook implemented, New Folder button functional, folders can be created via Directus API</done>
</task>

<task type="auto">
  <name>Task 2: Create file upload UI with Directus integration</name>
  <files>src/components/UploadFileDialog.tsx, src/hooks/useFiles.ts, src/pages/KnowledgeBase.tsx</files>
  <action>
    1. Create UploadFileDialog.tsx component using Shadcn/UI Dialog:
       - Dialog with "Upload File" title
       - File input (accept any file type, single file for v1)
       - Input for file title (optional - defaults to filename)
       - Textarea for description (optional)
       - Selected folder display (read-only - shows current folder)
       - Cancel and Upload buttons
       - Upload button triggers mutation with file + metadata
    2. In useFiles.ts, add useUploadFile mutation hook:
       - Use React Query useMutation
       - Create FormData with file, title, description, folder
       - Call Directus SDK: client.request(uploadFiles(formData))
       - Invalidate files query on success to refresh list
       - Show upload progress if possible (Directus SDK support)
       - Handle errors (file too large, invalid type, etc.)
    3. In KnowledgeBase.tsx:
       - Add "Upload File" button next to search bar (top-right of main content)
       - Button opens UploadFileDialog
       - Pass selectedFolderId to dialog (files upload to current folder)
       - Disable button if selectedFolderId is null (require folder selection)
       - Use Upload icon from lucide-react

    DO NOT implement drag-and-drop - simple file input is sufficient for v1.
    DO NOT implement multi-file upload - single file is sufficient for v1.
    Use Directus files endpoint: POST /files with multipart/form-data.
  </action>
  <verify>npm run build succeeds, Upload File button appears, clicking opens dialog, selecting file and uploading works</verify>
  <done>UploadFileDialog component created, useUploadFile hook implemented, Upload File button functional, files can be uploaded to Directus</done>
</task>

<task type="auto">
  <name>Task 3: Polish file management UX with validation and feedback</name>
  <files>src/components/CreateFolderDialog.tsx, src/components/UploadFileDialog.tsx</files>
  <action>
    1. CreateFolderDialog validation:
       - Disable Create button if folder name is empty
       - Trim whitespace from folder name before submission
       - Show loading state on Create button during mutation
       - Close dialog on successful creation
       - Display error message if creation fails (duplicate name, etc.)
    2. UploadFileDialog validation:
       - Disable Upload button if no file selected
       - Show file size and type after selection
       - Show loading state on Upload button during mutation
       - Display upload progress if available
       - Close dialog on successful upload
       - Display error message if upload fails (file too large, unsupported type, etc.)
    3. Both dialogs:
       - Reset form fields when dialog closes (canceled or completed)
       - Use proper ARIA labels for accessibility
       - Show success feedback (console.log for now, toast deferred)

    DO NOT implement toast notifications - console.log is sufficient (toast deferred to later phase).
    DO NOT add file size limits beyond Directus defaults (Nginx already has 50M limit).
  </action>
  <verify>Dialogs validate input, show loading states, handle errors gracefully, close on success</verify>
  <done>Folder creation validates name, file upload validates file selection, both show loading/error states, UX is polished</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] New Folder button appears in sidebar
- [ ] Upload File button appears in main content area
- [ ] CreateFolderDialog opens and creates folders via Directus
- [ ] UploadFileDialog opens and uploads files via Directus
- [ ] Created folders appear in folder tree immediately
- [ ] Uploaded files appear in file list immediately
- [ ] Error handling works for failed operations
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Users can create folders in hierarchy
- Users can upload files to folders
- Both operations integrated with Directus API
- Validation and loading states implemented
- No TypeScript errors
- UI is polished and user-friendly
</success_criteria>

<output>
After completion, create `.planning/phases/08-configuration-refactoring-ux-enhancements/08-05-SUMMARY.md`:

# Phase 8 Plan 5: Knowledge Base File Management Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/components/CreateFolderDialog.tsx` - Description
- `src/components/UploadFileDialog.tsx` - Description
- `src/hooks/useFolders.ts` - Description
- `src/hooks/useFiles.ts` - Description
- `src/pages/KnowledgeBase.tsx` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 8 complete, ready for Phase 9 (UI Overhaul)
</output>
