---
phase: 08-configuration-refactoring-ux-enhancements
plan: 04
type: execute
depends_on: []
files_modified: [src/components/Layout.tsx, src/pages/Chat.tsx, src/pages/KnowledgeBase.tsx, src/pages/Settings.tsx]
---

<objective>
Fix application layout and scrolling - fixed sidebar/header, scrollable content areas only.

Purpose: Current implementation may have scrolling issues where entire page scrolls instead of just content areas. Fix by ensuring sidebar and header are fixed (non-scrollable), while chat messages, knowledge base documents, and settings content scroll independently within their containers.
Output: Sidebar and header fixed in viewport, content areas scroll independently, no whole-page scrolling.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/Layout.tsx
@src/components/Sidebar.tsx
@src/components/Header.tsx
@src/pages/Chat.tsx
@src/pages/KnowledgeBase.tsx
@src/pages/Settings.tsx

**Tech stack available:** React, TypeScript, Tailwind CSS
**Established patterns:** Layout component wraps page content, Sidebar with fixed height
**Constraining decisions:**
- Phase 2: Layout with Sidebar (left), Header (top-right), and content area
- Current: Sidebar has h-screen, Chat has flex-col h-full with overflow-y-auto messages container

**Current implementation:**
- Layout.tsx wraps pages with Sidebar and Header
- Sidebar.tsx has h-screen (fixed height, full viewport)
- Header.tsx rendered in Layout
- Chat.tsx uses flex-col h-full with messagesContainerRef for scrolling
- KnowledgeBase.tsx has h-full flex with scrollable areas
- Settings.tsx has p-8 max-w-2xl (may scroll entire page if content long)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Layout component structure for fixed sidebar/header</name>
  <files>src/components/Layout.tsx</files>
  <action>
    1. Ensure Layout uses flex container with h-screen (full viewport height, no overflow)
    2. Sidebar should be fixed width (w-64) and fixed height (h-screen), no scrolling
    3. Right side container (header + content) should be flex-1 with flex-col and h-screen
    4. Header should have fixed height (h-20), no scrolling
    5. Content area should be flex-1 with overflow-hidden (children control their own scroll)
    6. Structure: <div flex h-screen><Sidebar /><div flex-1 flex-col h-screen><Header /><main flex-1 overflow-hidden>{children}</main></div></div>

    DO NOT add overflow-y-auto to main - let page components control their scrolling.
    DO NOT change Sidebar or Header components - only Layout structure.
  </action>
  <verify>npm run build succeeds, Layout renders without visual regressions</verify>
  <done>Layout creates fixed sidebar/header with overflow-hidden content container, no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Ensure page content areas scroll independently</name>
  <files>src/pages/Chat.tsx, src/pages/KnowledgeBase.tsx, src/pages/Settings.tsx</files>
  <action>
    1. Chat.tsx:
       - Root div should be h-full flex flex-col (fill parent, column layout)
       - Mode toggle section: fixed height, no scroll
       - Messages container: flex-1 overflow-y-auto (scrolls independently)
       - Chat input: fixed height, no scroll
       - Verify messagesContainerRef still works for auto-scroll
    2. KnowledgeBase.tsx:
       - Root div should be h-full flex (fill parent, row layout)
       - Folder sidebar: fixed width, overflow-y-auto (scrolls independently if long)
       - Main content: flex-1 overflow-y-auto (scrolls independently)
       - Detail panel: fixed position with overflow-y-auto (already correct)
    3. Settings.tsx:
       - Root div should be h-full overflow-y-auto (scrolls independently)
       - Keep existing p-8 max-w-2xl structure
       - Content scrolls within allocated space, not whole page

    DO NOT change scroll behavior of detail panels or modals (already work correctly).
    DO NOT break existing auto-scroll logic in Chat (messagesContainerRef.scrollTo).
  </action>
  <verify>All pages render correctly, content scrolls within bounds, sidebar/header stay fixed</verify>
  <done>Chat messages scroll independently, Knowledge Base scrolls independently, Settings scrolls independently, sidebar and header never scroll</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Sidebar remains fixed when scrolling page content
- [ ] Header remains fixed when scrolling page content
- [ ] Chat messages scroll within their container
- [ ] Knowledge Base documents scroll within their container
- [ ] Settings content scrolls within its container
- [ ] No whole-page scrolling occurs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Sidebar and header fixed in viewport
- Content areas scroll independently
- No layout shifts or visual regressions
- Existing functionality (auto-scroll, detail panels) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/08-configuration-refactoring-ux-enhancements/08-04-SUMMARY.md`:

# Phase 8 Plan 4: Fix Layout and Scrolling Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/components/Layout.tsx` - Description
- `src/pages/Chat.tsx` - Description
- `src/pages/KnowledgeBase.tsx` - Description
- `src/pages/Settings.tsx` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 08-05-PLAN.md (Knowledge Base file management)
</output>
