---
phase: 08-configuration-refactoring-ux-enhancements
plan: 01
type: execute
depends_on: []
files_modified: [src/pages/Settings.tsx, src/App.tsx, .env, USER_IMPUT_FILES/nginx.conf, src/lib/settings.ts]
---

<objective>
Remove Settings Page API configuration section and migrate to .env-only configuration with Nginx routing.

Purpose: Simplify configuration architecture by eliminating user-facing API endpoint settings. All endpoint configuration moves to .env with Nginx handling routing, reducing complexity and potential misconfiguration.
Output: Settings page with only Chat Preferences section, .env-based endpoint configuration, updated Nginx config if needed.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/Settings.tsx
@src/lib/settings.ts
@src/App.tsx
@.env
@USER_IMPUT_FILES/nginx.conf

**Tech stack available:** React, TypeScript, Tailwind CSS, Vite, React Router
**Established patterns:** .env variables with VITE_ prefix, localStorage for user preferences
**Constraining decisions:**
- Phase 6: API configuration stored in localStorage with 'api-settings' key
- Phase 7: Nginx proxy at ports 8080 (Directus) and 8081 (n8n)

**Current state:**
- Settings page has two sections: API Configuration (Directus URL, n8n Webhook URL) and Chat Preferences (message persistence toggle)
- lib/settings.ts handles loading/saving API settings from localStorage
- .env already has VITE_DIRECTUS_URL=http://localhost:8080
- Nginx config proxies Directus on port 8080, n8n on port 8081
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove API Configuration section from Settings page</name>
  <files>src/pages/Settings.tsx, src/lib/settings.ts</files>
  <action>
    1. In Settings.tsx, remove entire API Configuration Card (keep only Chat Preferences Card)
    2. Remove directusUrl and n8nWebhookUrl fields from formValues state
    3. Keep only messagePersistence field in formValues
    4. Update handleSave to only save messagePersistence to localStorage
    5. Update handleReset to only reset messagePersistence
    6. In lib/settings.ts, update ApiSettings interface to remove directusUrl and n8nWebhookUrl
    7. Keep loadSettings/saveSettings but simplify to only handle messagePersistence
    8. Remove DEFAULT_DIRECTUS_URL and DEFAULT_N8N_WEBHOOK_URL constants

    Do NOT remove the Settings route from App.tsx - the page still has Chat Preferences functionality.
  </action>
  <verify>npm run build succeeds, Settings page shows only Chat Preferences section</verify>
  <done>Settings page displays only Chat Preferences Card with message persistence toggle, no API configuration fields visible</done>
</task>

<task type="auto">
  <name>Task 2: Add n8n webhook URL to .env and update documentation</name>
  <files>.env, USER_IMPUT_FILES/nginx.conf</files>
  <action>
    1. Add VITE_N8N_WEBHOOK_URL=http://localhost:8081/webhook/chat to .env
    2. In nginx.conf comments (top of file), document that:
       - Frontend uses VITE_DIRECTUS_URL (port 8080) for all Directus requests
       - Frontend uses VITE_N8N_WEBHOOK_URL (port 8081) for n8n webhook
       - Nginx proxies these ports to actual services (directus:8055, n8n:5678)
    3. Verify that services using these URLs (useChatQuery, directus client initialization) will read from .env correctly

    DO NOT change any Nginx routing rules - they are already correct. Only add documentation comments and .env variable.
  </action>
  <verify>grep VITE_N8N_WEBHOOK_URL .env returns the new variable, nginx.conf has updated comments</verify>
  <done>.env contains both VITE_DIRECTUS_URL and VITE_N8N_WEBHOOK_URL, nginx.conf documents the routing architecture</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Settings page shows only Chat Preferences section
- [ ] .env has both VITE_DIRECTUS_URL and VITE_N8N_WEBHOOK_URL
- [ ] No references to directusUrl or n8nWebhookUrl in Settings page code
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Settings page simplified to Chat Preferences only
- All endpoint configuration in .env
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-configuration-refactoring-ux-enhancements/08-01-SUMMARY.md`:

# Phase 8 Plan 1: Remove Settings API Configuration Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/pages/Settings.tsx` - Description
- `src/lib/settings.ts` - Description
- `.env` - Description
- `USER_IMPUT_FILES/nginx.conf` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 08-02-PLAN.md (Markdown rendering)
</output>
