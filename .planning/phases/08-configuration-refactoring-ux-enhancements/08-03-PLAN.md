---
phase: 08-configuration-refactoring-ux-enhancements
plan: 03
type: execute
depends_on: []
files_modified: [src/hooks/useChat.ts, src/pages/Chat.tsx]
---

<objective>
Isolate chat contexts by mode - separate message history for RAG Search and LLM Chat.

Purpose: Users expect different conversation contexts for different modes. RAG Search is for document lookup (should not mix with general LLM conversations), LLM Chat is for general questions. Separate histories prevent context confusion and improve UX.
Output: Switching between RAG/LLM modes shows mode-specific message history, Clear History only clears current mode's messages.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/hooks/useChat.ts
@src/pages/Chat.tsx

**Tech stack available:** React, TypeScript, React Query, Directus SDK
**Established patterns:** React Query hooks for data fetching, Directus chat_messages collection
**Constraining decisions:**
- Phase 7.1: Chat messages stored in Directus with user foreign key
- Phase 4: RAG Search and LLM Chat modes with mode toggle
- Current: All messages share same history regardless of mode

**Current implementation:**
- useChatMessages fetches all messages for user (no mode filter)
- Chat.tsx shows all messages in single history
- Mode toggle switches mode but history remains shared
- Clear History deletes all messages for user
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update chat hooks to filter messages by mode</name>
  <files>src/hooks/useChat.ts</files>
  <action>
    1. Update useChatMessages hook to accept optional mode parameter:
       - Change signature: useChatMessages(userId: string, mode?: 'rag' | 'llm')
       - If mode provided, filter Directus query to only fetch messages matching that mode
       - Use Directus SDK filter: { user: { _eq: userId }, mode: { _eq: mode } }
       - If mode not provided (undefined), fetch all messages (maintain backward compatibility)
    2. Update useClearChatHistory to accept optional mode parameter:
       - Change signature: useClearChatHistory(userId: string, mode?: 'rag' | 'llm')
       - If mode provided, delete only messages matching userId AND mode
       - Use Directus SDK filter for deletion: { user: { _eq: userId }, mode: { _eq: mode } }
       - If mode not provided, delete all user messages (current behavior)
    3. Keep useSaveChatMessage unchanged - it already saves with mode field

    DO NOT change the Directus collection schema - mode field already exists.
    DO NOT break existing functionality - maintain backward compatibility with optional mode param.
  </action>
  <verify>npm run build succeeds, TypeScript compiles without errors</verify>
  <done>useChatMessages accepts mode filter, useClearChatHistory accepts mode filter, both maintain backward compatibility</done>
</task>

<task type="auto">
  <name>Task 2: Update Chat page to use mode-specific history</name>
  <files>src/pages/Chat.tsx</files>
  <action>
    1. Pass current mode to useChatMessages hook:
       - Change: const { data: messages = [], ... } = useChatMessages(user?.id, mode)
       - Now messages will automatically update when mode changes (React Query re-fetches on param change)
    2. Update handleClearHistory to clear only current mode:
       - Change: clearMutation.mutate({ userId: user.id, mode })
       - Update confirm message: `Clear ${mode === 'rag' ? 'RAG Search' : 'LLM Chat'} history?`
    3. Update useClearChatHistory call to pass both userId and mode
    4. Ensure mode state changes trigger re-fetch (React Query handles this automatically via query key)

    DO NOT add loading states for mode switching - React Query handles this with existing isLoading.
    DO NOT cache old mode's messages - let React Query handle fresh fetches per mode.
  </action>
  <verify>Switching modes shows different message histories, Clear History only clears current mode's messages</verify>
  <done>RAG mode shows only RAG messages, LLM mode shows only LLM messages, clearing history only affects current mode</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Switching between RAG/LLM modes shows different message histories
- [ ] Sending message in RAG mode doesn't appear in LLM mode
- [ ] Clear History button only clears current mode's messages
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- RAG and LLM histories completely isolated
- Clear History is mode-specific
- Mode switching works smoothly
- No regression in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/08-configuration-refactoring-ux-enhancements/08-03-SUMMARY.md`:

# Phase 8 Plan 3: Isolate Chat Contexts Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/hooks/useChat.ts` - Description
- `src/pages/Chat.tsx` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 08-04-PLAN.md (Layout and scrolling fixes)
</output>
