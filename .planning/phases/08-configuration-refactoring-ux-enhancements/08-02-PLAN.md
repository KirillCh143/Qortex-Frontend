---
phase: 08-configuration-refactoring-ux-enhancements
plan: 02
type: execute
depends_on: []
files_modified: [package.json, src/components/MessageBubble.tsx]
---

<objective>
Add Markdown rendering support for chat assistant responses.

Purpose: Enable rich formatting in LLM/RAG responses (headers, lists, code blocks, bold/italic) for better readability and structure. Assistant messages should support markdown while user messages remain plain text.
Output: MessageBubble component renders markdown for assistant messages, plain text for user messages.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/MessageBubble.tsx

**Tech stack available:** React, TypeScript, Tailwind CSS
**Established patterns:** Component-based architecture, role-based message rendering
**Constraining decisions:**
- Phase 4: MessageBubble displays user (cyan) and assistant (gray) messages differently
- Current: content rendered as plain text with whitespace-pre-wrap

**Current implementation:**
- MessageBubble receives role ('user' | 'assistant'), content (string), timestamp, mode
- User messages: cyan background, right-aligned
- Assistant messages: gray background, left-aligned
- Content displayed in paragraph with whitespace-pre-wrap class
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-markdown library</name>
  <files>package.json</files>
  <action>
    Install react-markdown and remark-gfm (GitHub Flavored Markdown support):

    npm install react-markdown remark-gfm

    Use react-markdown (most popular, 13M+ weekly downloads, actively maintained) instead of alternatives like markdown-to-jsx or marked. react-markdown provides safe-by-default rendering (no XSS), React component output, and extensive plugin ecosystem.

    remark-gfm adds support for tables, strikethrough, task lists, and autolinks - standard features users expect from markdown.
  </action>
  <verify>grep "react-markdown" package.json shows dependency, npm list react-markdown returns installed version</verify>
  <done>react-markdown and remark-gfm added to package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Implement markdown rendering in MessageBubble for assistant messages</name>
  <files>src/components/MessageBubble.tsx</files>
  <action>
    1. Import ReactMarkdown from 'react-markdown' and remarkGfm from 'remark-gfm'
    2. Replace the content paragraph rendering logic:
       - If role === 'user': render content as plain text (current behavior)
       - If role === 'assistant': render content using ReactMarkdown component with remarkGfm plugin
    3. Apply Tailwind prose classes to ReactMarkdown for good typography:
       - Use prose prose-sm max-w-none for base styling
       - Override prose colors to work with gray background: prose-headings:text-gray-900 prose-p:text-gray-900 prose-li:text-gray-900
       - Add prose-code:bg-gray-200 prose-code:px-1 prose-code:rounded for inline code
       - Use prose-pre:bg-gray-800 prose-pre:text-white for code blocks
    4. Keep text-sm and break-words classes for consistency
    5. Remove whitespace-pre-wrap from assistant messages (markdown handles formatting)

    DO NOT change user message rendering - keep plain text with whitespace-pre-wrap.
    DO NOT sanitize markdown - react-markdown is safe by default (no dangerouslySetInnerHTML).
  </action>
  <verify>npm run build succeeds, TypeScript compiles without errors</verify>
  <done>Assistant messages render markdown (headers, lists, code blocks work), user messages render plain text, no styling breaks</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] react-markdown installed in package.json
- [ ] User messages still render as plain text
- [ ] Assistant messages support markdown formatting
- [ ] Code blocks have dark background, inline code has light background
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Markdown rendering works for assistant messages
- User messages unchanged (plain text)
- No TypeScript errors
- Styling is consistent with existing design
</success_criteria>

<output>
After completion, create `.planning/phases/08-configuration-refactoring-ux-enhancements/08-02-SUMMARY.md`:

# Phase 8 Plan 2: Add Markdown Rendering Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `package.json` - Description
- `src/components/MessageBubble.tsx` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 08-03-PLAN.md (Isolated chat contexts)
</output>
