---
phase: 06-settings-panel
plan: 02
type: execute
depends_on: []
files_modified: [src/pages/Settings.tsx, src/pages/Chat.tsx, src/lib/chatStorage.ts]
---

<objective>
Implement message persistence toggle and chat preference controls.

Purpose: Give users control over whether chat history persists across sessions, supporting both persistent research workflows and ephemeral quick queries.
Output: Settings toggle for message persistence that Chat page respects, with visual feedback showing active persistence status.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/Settings.tsx
@src/pages/Chat.tsx
@src/lib/chatStorage.ts
@src/components/ui/card.tsx
@src/components/ui/button.tsx

**Tech stack available:** React, TypeScript, Tailwind CSS, Shadcn/UI, Lucide Icons
**Established patterns:**
- localStorage service pattern (chatStorage.ts, settings.ts)
- Settings form UI pattern (established in 06-01)
- Chat page message state management (Phase 4)
- Shadcn Switch component for toggles

**Constraining decisions:**
- Phase 4: Message history uses localStorage via chatStorage.ts
- Phase 4: Messages load on Chat page mount, save on every new message
- Phase 6 Plan 1: Settings use Card layout with Save/Reset buttons

**Requirements from PROJECT.md:**
- Message history persistence must be user-configurable
- Default should be enabled (most users want history)
- Clear indication of persistence status in chat interface
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add message persistence toggle to Settings</name>
  <files>src/lib/settings.ts, src/pages/Settings.tsx</files>
  <action>
Update ApiSettings interface in settings.ts to include messagePersistence: boolean. Update default values to include messagePersistence: true (default enabled). Update saveSettings/loadSettings to handle the new field. In Settings.tsx, add a new Card section titled "Chat Preferences" below the API Configuration section. Add a Shadcn Switch component labeled "Persist Message History" with helper text: "When enabled, chat messages are saved and restored across sessions. When disabled, chat clears on page refresh." Connect Switch to form state and save alongside API settings. Add spacing between the two settings sections using mt-6.
  </action>
  <verify>npm run build succeeds. Settings page shows both API Configuration and Chat Preferences sections. Toggle saves to localStorage when Save Settings clicked.</verify>
  <done>Settings page includes message persistence toggle in separate section, saves preference to localStorage, loads preference on mount</done>
</task>

<task type="auto">
  <name>Task 2: Update Chat page to respect persistence setting</name>
  <files>src/pages/Chat.tsx, src/lib/chatStorage.ts</files>
  <action>
Update Chat.tsx to conditionally load/save messages based on persistence setting. Import loadSettings from '@/lib/settings'. On mount (useEffect), check if messagePersistence is true before calling loadMessages() - if false, start with empty array. When saving messages after user/assistant interaction, check messagePersistence setting - only call saveMessages() if true. Add function clearMessagesIfDisabled() that runs on mount: if persistence is false and messages exist in localStorage, clear them. This ensures clean state when user disables persistence. Do NOT modify the Message interface or existing message rendering logic - only change the persistence behavior.
  </action>
  <verify>npm run build succeeds. Chat page compiles without TypeScript errors. Messages only persist when setting is enabled.</verify>
  <done>Chat page conditionally loads/saves messages based on messagePersistence setting, clears stale messages when persistence disabled</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Message persistence toggle with Settings integration and Chat page conditional storage</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Navigate to Settings page (http://localhost:5173/settings)
    3. Verify: API Configuration section and Chat Preferences section both visible
    4. Verify: Message persistence toggle is present and ON by default
    5. Navigate to Chat page, send a few messages
    6. Refresh page - messages should persist (toggle is ON)
    7. Go to Settings, toggle persistence OFF, click Save Settings
    8. Navigate to Chat, send new messages
    9. Refresh page - messages should be cleared (toggle is OFF)
    10. Go to Settings, toggle persistence ON, click Save Settings
    11. Navigate to Chat, send messages
    12. Refresh page - messages should persist again (toggle is ON)
    13. Verify: No console errors in any scenario
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] npm run build succeeds without errors
- [ ] Settings page displays both API Configuration and Chat Preferences
- [ ] Message persistence toggle saves to localStorage
- [ ] Chat page respects persistence setting (loads/saves conditionally)
- [ ] Disabling persistence clears stale messages
- [ ] No TypeScript errors or console warnings
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Message persistence toggle works correctly
- Chat behavior changes based on setting
- Phase 6 complete: Settings panel fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-panel/06-02-SUMMARY.md`:

# Phase 6 Plan 2: Chat Preferences & Persistence Control Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/lib/settings.ts` - Description
- `src/pages/Settings.tsx` - Description
- `src/pages/Chat.tsx` - Description
- `src/lib/chatStorage.ts` - Description (if modified)

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 6 complete. Ready for Phase 7: API Integration Layer (connect to real Directus and n8n with mock/real toggle)
</output>
