---
phase: 06-settings-panel
plan: 01
type: execute
depends_on: []
files_modified: [src/lib/settings.ts, src/pages/Settings.tsx, src/lib/directus.ts]
---

<objective>
Build API configuration interface for user-controlled Directus and n8n endpoint URLs.

Purpose: Enable users to configure backend endpoints without rebuilding the app, supporting flexible localhost and production deployments.
Output: Working settings page with API URL inputs that persist to localStorage and integrate with Directus client.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/directus.ts
@src/lib/chatStorage.ts
@src/pages/Settings.tsx
@src/components/ui/card.tsx
@src/components/ui/button.tsx

**Tech stack available:** React, TypeScript, Tailwind CSS, Shadcn/UI, Directus SDK
**Established patterns:**
- localStorage service pattern (see chatStorage.ts for reference)
- TypeScript interfaces for data structures
- Shadcn/UI Card and Button components for forms
- Environment variable fallbacks (see directus.ts for VITE_DIRECTUS_URL pattern)

**Constraining decisions:**
- Phase 1: Path aliases (@/) configured for imports
- Phase 1: Shadcn/UI for all UI components
- Phase 4: localStorage pattern for persistence established in chatStorage.ts
- Phase 5: Settings page exists as placeholder, needs implementation

**Mock-first approach:** This phase works with real localStorage persistence from the start - no mock mode needed for settings storage.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create settings storage service and TypeScript types</name>
  <files>src/lib/settings.ts</files>
  <action>
Create settings.ts with TypeScript interface for ApiSettings (directusUrl: string, n8nWebhookUrl: string). Implement saveSettings() and loadSettings() functions following the pattern from chatStorage.ts. Use storage key 'api-settings'. Include default values: directusUrl defaults to window.location.origin in dev or 'http://localhost:8055' in production, n8nWebhookUrl defaults to 'http://localhost:5678/webhook/chat'. Add proper error handling with console.error for storage failures. Export both the interface and functions.
  </action>
  <verify>TypeScript compilation passes with no errors. Can import { ApiSettings, saveSettings, loadSettings } from '@/lib/settings'</verify>
  <done>settings.ts exists with ApiSettings interface, saveSettings/loadSettings functions, default values, and error handling</done>
</task>

<task type="auto">
  <name>Task 2: Build API configuration form UI</name>
  <files>src/pages/Settings.tsx</files>
  <action>
Replace placeholder Settings page with functional form. Use Shadcn Card component for layout. Create two input fields: "Directus URL" and "n8n Webhook URL" using Shadcn Input component. Add useState for form values, load initial values from loadSettings() in useEffect. Add "Save Settings" button (Shadcn Button, primary variant) that calls saveSettings() and shows toast notification on success (use console.log for now - toast can be added in Phase 8). Add "Reset to Defaults" button (secondary variant) that clears localStorage and reloads defaults. Include helper text under each input explaining what it's for (gray text, text-sm). Use the cyan accent color (#06b6d4) for the Save button active state to match project palette.
  </action>
  <verify>npm run build succeeds. Settings page renders with two input fields, Save and Reset buttons. Form loads values from localStorage on mount.</verify>
  <done>Settings.tsx displays API configuration form with Directus URL and n8n URL inputs, Save/Reset buttons, loads persisted settings, saves to localStorage on submit</done>
</task>

<task type="auto">
  <name>Task 3: Integrate settings into Directus client</name>
  <files>src/lib/directus.ts</files>
  <action>
Update directus.ts to check localStorage for user-configured Directus URL before falling back to environment variable. Import loadSettings from './settings'. Change BACKEND_URL logic: first try loadSettings().directusUrl (if not empty), then fall back to existing logic (VITE_DIRECTUS_URL env var, then DEV ternary). This ensures user settings take precedence over defaults. Do NOT change the authentication or rest configuration. Keep the existing DirectusAuth interface and client export unchanged. Add comment explaining the precedence: user settings > env var > dev/prod defaults.
  </action>
  <verify>TypeScript compilation passes. Directus client still exports correctly. Import chain works: directus.ts imports settings.ts.</verify>
  <done>directus.ts reads directusUrl from localStorage settings with proper fallback chain, preserves all existing functionality</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Settings page displays API configuration form
- [ ] Saving settings persists to localStorage
- [ ] Reset button clears settings and restores defaults
- [ ] directus.ts imports and uses settings correctly
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Settings storage service follows chatStorage.ts pattern
- API configuration form uses Shadcn/UI components
- Directus client respects user-configured URLs
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-panel/06-01-SUMMARY.md`:

# Phase 6 Plan 1: API Configuration Settings Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `src/lib/settings.ts` - Description
- `src/pages/Settings.tsx` - Description
- `src/lib/directus.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 06-02-PLAN.md (Chat Preferences & Persistence Control)
</output>
