---
phase: 7.5-login-page-design-overhaul-and-redirect-fix
plan: 01
type: execute
depends_on: []
files_modified: [src/pages/Login.tsx]
---

<objective>
Polish login page with branded logo and prevent authenticated users from accessing login page.

Purpose: Improve visual brand consistency and fix UX issue where logged-in users can navigate back to login page.
Output: Login page with robot logo and automatic redirect for authenticated users.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/Login.tsx
@src/contexts/AuthContext.tsx
@USER_IMPUT_FILES/TO_DO.md
@USER_IMPUT_FILES/Design/Login/code.html
@USER_IMPUT_FILES/Design/Login/screen.png

**From USER_IMPUT_FILES/TO_DO.md:**
- Redirect from login page if auth is OK

**From USER_IMPUT_FILES/Design/Login/:**
- Reference HTML shows complete modern login design with white background, centered card, gradient logo circle
- Purple color scheme (#8466e4) with gradient to #a18cf0
- Russian text: "Nexus AI" title, "Вход в систему" subtitle, "Войти" button
- Modern rounded corners (rounded-2xl for inputs, rounded-3xl for card)
- Soft shadows and focus rings
- Fixed footer at bottom right
- User wants identical implementation

**From codebase:**
- AuthContext provides isAuthenticated and loading state
- ProtectedRoute handles redirect to /login for unauthenticated users
- Login page needs inverse logic: redirect authenticated users to /chat
- Lucide React icons available (will use Atom icon)

**New color approach:**
- Primary: #8466e4 (Purple - matches reference design)
- Gradient: from-primary to-[#a18cf0]
- Replacing old blue theme with modern purple/white design
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete login page redesign matching reference design</name>
  <files>src/pages/Login.tsx</files>
  <action>
Completely redesign the login page to match the reference design in USER_IMPUT_FILES/Design/Login/:

**Layout & Background:**
1. Change outer container from `bg-primary` to `bg-white`
2. Update card styling to match reference:
   - max-w-[440px] (instead of max-w-md)
   - rounded-3xl (instead of rounded-lg)
   - shadow-[0_20px_50px_rgba(0,0,0,0.05)] (modern soft shadow)
   - border border-slate-100
   - p-10 (instead of p-8)

**Logo Section:**
3. Replace Building2 import with Atom icon from lucide-react: `import { Atom } from 'lucide-react'`
4. Create gradient circle container for icon:
   ```jsx
   <div className="size-14 rounded-2xl bg-gradient-to-br from-primary to-[#a18cf0] flex items-center justify-center text-white shadow-xl shadow-primary/20 mb-5">
     <Atom className="h-8 w-8" />
   </div>
   ```
5. Update branding text:
   - h1: "Nexus AI" with `text-2xl font-bold text-slate-900 tracking-tight mb-1`
   - p: "Вход в систему" (Russian) with `text-slate-500 font-medium`

**Form Styling:**
6. Update form container: `space-y-6` (instead of space-y-4)
7. Update label styling: `block text-sm font-semibold text-slate-700 mb-2 ml-1`
8. Update input styling (both email and password):
   ```jsx
   className="w-full bg-slate-50/50 border border-slate-200/60 rounded-2xl px-5 py-3.5 text-slate-900 placeholder-slate-400 focus:outline-none focus:ring-4 focus:ring-primary/10 focus:border-primary transition-all"
   ```
9. Update placeholders: email: "example@nexus.ai", password: "••••••••"
10. Update button styling:
    ```jsx
    className="w-full bg-primary hover:bg-primary/90 text-white font-bold py-4 rounded-2xl shadow-lg shadow-primary/20 transition-all transform active:scale-[0.98]"
    ```
11. Update button text: "Войти" (Russian for "Sign in")

**Footer:**
12. Add fixed footer at bottom right (outside the card, inside the outer container):
    ```jsx
    <footer className="fixed bottom-6 right-8 z-10">
      <p className="text-slate-400 text-[11px] font-medium tracking-widest uppercase">
        © 2024 NEXUS AI. ВСЕ ПРАВА ЗАЩИЩЕНЫ.
      </p>
    </footer>
    ```

**Key Points:**
- The primary color (#8466e4 purple) should already exist in tailwind config or use the reference color
- Use rounded-2xl for inputs/button, rounded-3xl for card
- Focus states use ring-4 with ring-primary/10 for subtle effect
- Gradient is from-primary to-[#a18cf0] for the logo circle
- All text follows the reference design exactly (Russian text included)

Avoid: Don't keep the old blue background. Don't use Building2 icon. Don't use generic "Company Name" text. Don't skip the footer. Don't use different rounded values - match the reference exactly.
  </action>
  <verify>Visual inspection comparing with USER_IMPUT_FILES/Design/Login/screen.png - layout, colors, spacing, shadows should match identically</verify>
  <done>Login page matches reference design with white background, centered card, Atom icon in gradient circle, modern inputs, Russian text, and footer</done>
</task>

<task type="auto">
  <name>Task 2: Add redirect for authenticated users</name>
  <files>src/pages/Login.tsx</files>
  <action>
Add logic to redirect authenticated users away from login page:
1. Import useEffect and useNavigate (already imported)
2. Get isAuthenticated and loading from useAuth() (already destructured)
3. Add useEffect hook that runs when isAuthenticated or loading changes:
   - If !loading && isAuthenticated, navigate('/chat', { replace: true })
4. Place this effect near the top of the component after state declarations
5. Use replace: true to prevent back button issues (same pattern as ProtectedRoute)

This prevents the UX issue where logged-in users can manually navigate to /login and see the login form.

Avoid: Don't redirect while loading is true (causes flash). Don't use window.location (use navigate for React Router consistency).
  </action>
  <verify>
1. Log in successfully and navigate to /chat
2. Manually type localhost:5173/login in browser
3. Should immediately redirect back to /chat (no login form flash)
4. Log out and verify /login page is accessible for unauthenticated users
  </verify>
  <done>Authenticated users accessing /login are redirected to /chat; unauthenticated users see login form</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Completely redesigned login page matching reference design + authenticated user redirect</what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Clear localStorage to ensure logged out state
3. Visit http://localhost:5173/login
4. **Design verification** - Compare with USER_IMPUT_FILES/Design/Login/screen.png:
   - White background (not blue)
   - Centered card with soft shadow and border
   - Atom icon in purple gradient circle at top
   - "Nexus AI" title with "Вход в систему" subtitle
   - Modern rounded inputs with slate backgrounds
   - Purple button with shadow and "Войти" text
   - Footer "© 2024 NEXUS AI. ВСЕ ПРАВА ЗАЩИЩЕНЫ." at bottom right
5. **Interaction verification:**
   - Click inputs - should show purple focus ring
   - Hover button - should darken slightly
   - Click button - should scale down slightly (active:scale-[0.98])
6. **Auth redirect verification:**
   - Log in with valid credentials
   - After login redirects to /chat, manually navigate to /login
   - Should immediately redirect back to /chat (no login form visible)
   - Log out and verify /login is accessible when not authenticated
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Login page matches reference design (USER_IMPUT_FILES/Design/Login/screen.png)
- [ ] White background with centered card styling
- [ ] Atom icon in purple gradient circle
- [ ] "Nexus AI" and Russian text displayed correctly
- [ ] Modern input styling with focus rings working
- [ ] Purple button with hover/active states working
- [ ] Footer displayed at bottom right
- [ ] Authenticated users cannot access /login (auto-redirected to /chat)
- [ ] Unauthenticated users can access /login normally
- [ ] No console errors or warnings
- [ ] Build completes without errors: `npm run build`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Login page matches reference design identically
- Modern purple/white theme implemented
- Authenticated users auto-redirect from /login to /chat
- UX improvement complete per USER_IMPUT_FILES/TO_DO.md
- Russian branding text displays correctly
  </success_criteria>

<output>
After completion, create `.planning/phases/7.5-login-page-design-overhaul-and-redirect-fix/7.5-01-SUMMARY.md`:

# Phase 7.5 Plan 1: Login Page Design Overhaul and Redirect Fix Summary

**Login page completely redesigned with modern purple/white theme matching reference design and authenticated user redirect**

## Accomplishments

- Complete visual overhaul: white background, centered card with soft shadows
- Replaced Building2 icon with Atom icon in purple gradient circle
- Implemented "Nexus AI" branding with Russian text ("Вход в систему", "Войти")
- Modern form inputs with slate backgrounds and purple focus rings
- Purple button with hover/active states and shadows
- Added fixed footer with copyright text
- Added automatic redirect for authenticated users attempting to access /login
- Fixed UX issue from USER_IMPUT_FILES/TO_DO.md

## Files Created/Modified

- `src/pages/Login.tsx` - Complete redesign matching USER_IMPUT_FILES/Design/Login/ reference, added useEffect for authenticated redirect

## Decisions Made

- Purple color scheme (#8466e4) replaces old blue theme for login page
- Gradient from-primary to-[#a18cf0] for logo circle background
- Russian text throughout (matching reference design exactly)
- Rounded-2xl for inputs/button, rounded-3xl for card
- Focus states use ring-4 with ring-primary/10 for subtle effect
- Used navigate with replace: true to prevent back button issues (consistent with ProtectedRoute pattern)
- Redirect triggers only when !loading && isAuthenticated to avoid flash during auth check

## Issues Encountered

None

## Next Phase Readiness

Phase 7.5 complete. All INSERTED phases finished. Ready for Phase 8: Polish & Testing.
</output>
