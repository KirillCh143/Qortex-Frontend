---
phase: 05-knowledge-base-interface
plan: 01
type: execute
depends_on: []
files_modified: [
  src/pages/KnowledgeBase.tsx,
  src/lib/mockDocuments.ts
]
---

<objective>
Build document browsing interface with list view, detail view, and view/download capabilities using mock data.

Purpose: Enable employees to browse internal documentation catalog and access document files, validating UX before Directus Files API integration in Phase 7.
Output: Functional knowledge base page with document list, search/filter, detail display, and view/download buttons.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-chat-interface-mock/04-01-SUMMARY.md
@src/pages/KnowledgeBase.tsx
@src/pages/Chat.tsx
@src/lib/mockResponses.ts
@src/lib/chatStorage.ts

**Tech stack available:**
- Shadcn/UI components (Button, Avatar, Card)
- Lucide-react icons
- React Router for navigation
- localStorage for persistence patterns
- Tailwind CSS for styling

**Established patterns from Phase 4:**
- Mock data pattern: Separate lib file with type definitions and mock array
- localStorage persistence: load on mount, save on changes
- Component structure: List container with scroll, individual item components
- Color palette: Deep blue primary, cyan accents (#06b6d4)

**Key decisions constraining this phase:**
- Phase 4: Mock-first approach with 500ms delay to simulate async operations
- Phase 4: localStorage pattern for state persistence
- Phase 2: Layout uses max-w-4xl mx-auto for content containers
- Phase 2: Color palette (cyan accents, gray backgrounds)

**Requirements from PROJECT.md:**
- Knowledge base document browsing (view/download capabilities)
- Mock-first UI development with easy toggle between mock/real data modes

**Phase 7 connection:**
Real data will come from Directus Files API (`directus_files` collection). Mock data structure should mirror expected API response: id, title, filename, filesize, uploaded_on, description.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mock document data and list view UI</name>
  <files>src/lib/mockDocuments.ts, src/pages/KnowledgeBase.tsx</files>
  <action>
1. Create `src/lib/mockDocuments.ts`:
   - Type definition: `Document { id: string, title: string, filename: string, filesize: number, uploadedOn: Date, description: string, category: string }`
   - Export array of 8-10 mock documents with realistic data (Employee Handbook.pdf, Technical Guidelines.md, etc.)
   - Include mix of PDF/MD/DOCX files, various categories (HR, Engineering, Operations)
   - Filesizes in bytes (use realistic values: 245000 for ~245KB)

2. Update `src/pages/KnowledgeBase.tsx`:
   - Import mock documents and useState/useEffect
   - State: documents (all docs), filteredDocuments (search results), selectedDoc (detail view), searchQuery
   - Header section: Title "Knowledge Base", subtitle "Browse Internal Documentation"
   - Search bar: Input with Search icon, placeholder "Search documents...", onChange filters by title/description
   - Document list: Grid layout (grid-cols-1 md:grid-cols-2 gap-4) with cards for each document
   - Each card: Title (bold), category badge (gray-100 bg), description (truncate 2 lines), filesize (human readable: use helper function), uploaded date (format: MMM DD, YYYY)
   - Card hover: Border color changes to cyan-500, cursor pointer
   - onClick card: Set selectedDoc to open detail view
   - Empty state: "No documents found" when filteredDocuments is empty

Do NOT implement detail view yet (that's Task 2). Do NOT add download functionality yet.

Use Shadcn/UI Card component. Follow Phase 4 patterns: max-w-4xl container, p-4 spacing, gray-100 backgrounds for cards.
  </action>
  <verify>
1. npm run build succeeds without errors
2. Navigate to /knowledge-base route
3. Document list displays with all mock documents
4. Search input filters documents by title/description
5. Cards show all metadata correctly formatted
6. Hover states work (border color change)
7. Click card updates selectedDoc state (verify in React DevTools)
  </verify>
  <done>Document list view displays 8-10 mock documents with search, category badges, metadata display, and click handling. No TypeScript errors, follows established UI patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Implement document detail view with view/download buttons</name>
  <files>src/pages/KnowledgeBase.tsx</files>
  <action>
Add detail view panel that appears when selectedDoc is set:

1. Conditional rendering: `{selectedDoc && <DetailPanel />}`
2. Detail panel structure:
   - Modal overlay (fixed inset-0 bg-black/50 z-50) with onClick close
   - Panel container (fixed right-0 top-0 h-full w-full md:w-1/2 bg-white shadow-xl z-50 overflow-y-auto)
   - Close button (top-right, X icon from lucide-react)
   - Document header: Title (text-2xl font-bold), category badge, uploaded date
   - Metadata section: Filename, filesize (human readable), uploaded by (mock: "System Admin")
   - Description section: Full description text (text-gray-700)
   - Action buttons (flex gap-2):
     * View button (Button variant="default" with cyan bg): Opens alert "View functionality will be implemented in Phase 7"
     * Download button (Button variant="outline" with Download icon): Opens alert "Download functionality will be implemented in Phase 7"

3. Keyboard handling:
   - Escape key closes detail view (useEffect with keydown listener, cleanup)
   - Click outside panel closes detail view (onClick on overlay)

4. Styling:
   - Follow Phase 4 patterns: cyan accent colors for primary actions
   - Use Shadcn/UI Button, lucide-react icons (Download, X)
   - Panel slides in from right with smooth transition (transition-transform duration-300)
   - Responsive: Full width on mobile, half width on desktop

Do NOT implement real view/download functionality - use alert() placeholders referencing Phase 7. Focus on UX flow and visual polish.
  </action>
  <verify>
1. npm run build succeeds without errors
2. Click document card opens detail panel
3. Detail panel displays all document metadata correctly
4. Close button (X) closes panel
5. Escape key closes panel
6. Click overlay (outside panel) closes panel
7. View button shows alert with Phase 7 message
8. Download button shows alert with Phase 7 message
9. Panel is responsive (full width mobile, half width desktop)
10. Transitions are smooth (slide in from right)
  </verify>
  <done>Detail view panel fully functional with all metadata display, close interactions (button/escape/overlay), and placeholder view/download buttons. Responsive, smooth transitions, follows UI patterns from Phase 4.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] npm run build succeeds without errors
- [ ] npm run lint shows no new errors
- [ ] Navigate to /knowledge-base displays document list
- [ ] Search filters documents correctly
- [ ] Click document opens detail panel
- [ ] All close interactions work (X, Escape, overlay click)
- [ ] View/Download buttons show Phase 7 placeholder alerts
- [ ] UI follows established patterns (colors, spacing, responsiveness)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript or ESLint errors introduced
- Knowledge base page displays mock documents with search/filter
- Detail view shows all metadata with view/download placeholders
- Responsive design works on desktop and mobile
- Phase 5 complete, ready for Phase 6 (Settings Panel)
</success_criteria>

<output>
After completion, create `.planning/phases/05-knowledge-base-interface/05-01-SUMMARY.md`:

# Phase 5 Plan 1: Knowledge Base Interface Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]
- [Key outcome 3]

## Files Created/Modified

- `src/lib/mockDocuments.ts` - Description
- `src/pages/KnowledgeBase.tsx` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

**Phase 5 Complete** - Knowledge base interface fully functional with mock data, ready for Phase 6 (Settings Panel).
</output>
