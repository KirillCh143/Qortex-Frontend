---
phase: 10-knowledgebase-page-edits
plan: 01
type: execute
depends_on: []
files_modified: [
  src/services/directus/types.ts,
  src/services/directus/files.service.ts,
  src/services/mock/files.mock.ts,
  src/lib/mockDocuments.ts,
  src/pages/KnowledgeBase.tsx,
  src/components/FileListView.tsx
]
---

<objective>
Enhance file tiles with complete metadata display and Russian date localization across grid and list views.

Purpose: Display comprehensive file information (uploader name, Russian dates, file size, file type) in both grid and list views using a consistent tile design for better UX.
Output: Enhanced file tiles showing all metadata, list view redesigned to use tile layout, all dates in Russian.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-knowledgebase-page-edits/10-CONTEXT.md
@src/services/directus/types.ts
@src/services/directus/files.service.ts
@src/lib/mockDocuments.ts
@src/pages/KnowledgeBase.tsx
@src/components/FileListView.tsx

**Key Context:**
- Directus Files API has `user_created` field (system field) that contains uploader user ID
- Need to expand user data to get uploader name (first_name, last_name)
- Grid view already has file type icons with colored backgrounds
- List view currently uses table layout - needs complete redesign to single-column tile layout
- Russian localization required for all date displays (months in Russian)

**Established Patterns:**
- Purple theme (#8466e4) for primary actions
- File type icons: PDF (red), DOCX (blue), XLSX (green), text (gray)
- Russian text throughout UI from Phase 9
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add uploader user data to DirectusFile type and fetch logic</name>
  <files>src/services/directus/types.ts, src/services/directus/files.service.ts, src/services/mock/files.mock.ts</files>
  <action>
    1. Update DirectusFile interface in types.ts to include:
       - user_created: { id: string; first_name: string; last_name: string } | null

    2. Update files.service.ts getFiles() method:
       - Add 'user_created.id', 'user_created.first_name', 'user_created.last_name' to fields array
       - This expands the user_created relation to get uploader name

    3. Update mock files.mock.ts to include mock user_created data:
       - Add user_created: { id: 'mock-user-1', first_name: 'System', last_name: 'Admin' } to mock files
       - Ensures consistent structure with real Directus response

    4. Update mockDocuments.ts Document interface to include:
       - uploadedBy: string (for display purposes)
       - Set to 'System Admin' for all mock documents
  </action>
  <verify>TypeScript compiles without errors, no type mismatches in KnowledgeBase.tsx</verify>
  <done>DirectusFile type includes user_created with name fields, API fetches user data, mock service returns user data</done>
</task>

<task type="auto">
  <name>Task 2: Create Russian date formatting utility function</name>
  <files>src/lib/mockDocuments.ts</files>
  <action>
    Replace existing formatDate() function with formatDateRussian() that:
    1. Takes Date object as input
    2. Returns Russian-formatted date string: "DD месяц YYYY" format
    3. Month mapping (English → Russian):
       - January → января, February → февраля, March → марта
       - April → апреля, May → мая, June → июня
       - July → июля, August → августа, September → сентября
       - October → октября, November → ноября, December → декабря
    4. Use lowercase for Russian month names (genitive case)
    5. Example output: "15 января 2024" instead of "Jan 15, 2024"

    Keep formatFileSize() function unchanged - it already works correctly.

    Export both formatDateRussian and formatFileSize for use in components.
  </action>
  <verify>Import formatDateRussian in KnowledgeBase.tsx and FileListView.tsx, dates display in Russian</verify>
  <done>Russian date formatter implemented, month names in Russian genitive case, all dates formatted consistently</done>
</task>

<task type="auto">
  <name>Task 3: Enhance grid view tiles with complete metadata</name>
  <files>src/pages/KnowledgeBase.tsx</files>
  <action>
    Update grid view tile rendering (around line 260-292):

    1. Replace current metadata line (line 284-286) with enhanced metadata:
       - Line 1: Uploader name from file.user_created (use "Неизвестный" if null)
         Format: "Загрузил: {first_name} {last_name}"
       - Line 2: Upload date in Russian + file size + file type label
         Format: "{formatDateRussian(uploaded_on)} • {formatFileSize(filesize)} • {fileTypeInfo.label}"

    2. Keep existing structure:
       - File type icon with colored background (already correct)
       - File name with AutoScrollTitle
       - Enhanced metadata below

    3. Styling:
       - Uploader: text-sm text-gray-600 font-medium
       - Date/size/type: text-xs text-gray-500
       - Use • (bullet) as separator between date, size, and type

    Example output:
    ```
    [Icon]
    Document Title
    Загрузил: System Admin
    15 января 2024 • 2.5 MB • PDF
    ```
  </action>
  <verify>Grid view displays uploader name, Russian dates, file size, and file type for all files</verify>
  <done>Grid tiles show complete metadata: uploader name, Russian date, file size, file type label</done>
</task>

<task type="auto">
  <name>Task 4: Redesign list view to use tile layout</name>
  <files>src/components/FileListView.tsx</files>
  <action>
    Complete redesign of FileListView component from table to single-column tile layout:

    1. Remove entire table structure (thead, tbody, tr, td elements)

    2. Replace with div-based tile layout:
       - Container: flex flex-col gap-3 (vertical stack with spacing)
       - Each file tile: Card component with same design as grid view

    3. Tile structure (horizontal layout within each tile):
       ```tsx
       <Card key={file.id} onClick={() => onSelectFile(file)} className="...">
         <CardContent className="p-4 flex items-center gap-4">
           {/* Checkbox on far left */}
           <Checkbox onClick={(e) => e.stopPropagation()} />

           {/* File icon (same as grid) */}
           <div className={`w-12 h-12 rounded-lg ${bgColor} flex items-center justify-center`}>
             <Icon className={`h-6 w-6 ${textColor}`} />
           </div>

           {/* File info (flex-1 to take remaining space) */}
           <div className="flex-1 min-w-0">
             <h3 className="font-semibold text-gray-900 truncate">{file.title}</h3>
             <p className="text-sm text-gray-600">Загрузил: {uploader name}</p>
             <p className="text-xs text-gray-500">{Russian date} • {file size} • {file type}</p>
           </div>

           {/* Download icon on far right */}
           <Download className="h-5 w-5 text-gray-400" />
         </CardContent>
       </Card>
       ```

    4. Styling:
       - Card: cursor-pointer hover:border-[#8466e4] transition-all
       - Same purple hover effect as grid view
       - Responsive: full width on all screen sizes

    5. Import formatDateRussian and formatFileSize from @/lib/mockDocuments

    6. Extract uploader name from file.user_created (use "Неизвестный" if null)

    Result: List view uses identical tile design to grid view, just stacked vertically (one per row) instead of multi-column grid.
  </action>
  <verify>List view displays files as vertical stack of tiles, same metadata as grid view, smooth hover effects</verify>
  <done>List view redesigned to single-column tile layout, visual consistency with grid view, complete metadata displayed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles without errors
- [ ] Grid view tiles show uploader name, Russian dates, file size, file type
- [ ] List view uses tile layout (not table), same metadata as grid
- [ ] All dates display in Russian (e.g., "15 января 2024" not "Jan 15, 2024")
- [ ] Toggle between grid and list views works smoothly
- [ ] Visual consistency between grid and list views
- [ ] No console errors or warnings
</verification>

<success_criteria>

- All tasks completed
- Grid and list views display complete file metadata
- Russian date localization throughout
- Visual consistency between view modes
- No TypeScript errors or runtime warnings
</success_criteria>

<output>
After completion, create `.planning/phases/10-knowledgebase-page-edits/10-01-SUMMARY.md`:

# Phase 10 Plan 1: Enhanced File Tiles with Metadata Summary

**Enhanced file browsing with complete metadata display and Russian localization**

## Accomplishments

- Added uploader user data to DirectusFile type with relation expansion
- Created Russian date formatting utility (genitive case month names)
- Enhanced grid view tiles with uploader name, Russian dates, file size, file type
- Redesigned list view from table to single-column tile layout
- Achieved visual consistency between grid and list views

## Files Created/Modified

- `src/services/directus/types.ts` - Added user_created relation to DirectusFile
- `src/services/directus/files.service.ts` - Expanded user_created in API query
- `src/services/mock/files.mock.ts` - Added mock uploader data
- `src/lib/mockDocuments.ts` - Created formatDateRussian function
- `src/pages/KnowledgeBase.tsx` - Enhanced grid view tile metadata
- `src/components/FileListView.tsx` - Complete redesign to tile layout

## Decisions Made

- Used genitive case for Russian month names (января, февраля, etc.)
- List view tiles horizontal layout (icon left, metadata center, download right)
- Uploader fallback text: "Неизвестный" for null user_created
- Separator: • (bullet) between date, size, and type

## Issues Encountered

None

## Next Step

Ready for 10-02-PLAN.md (Detail panel improvements)
</output>
