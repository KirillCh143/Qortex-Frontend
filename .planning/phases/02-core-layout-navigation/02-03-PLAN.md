---
phase: 02-core-layout-navigation
plan: 03
type: execute
depends_on: ["02-01", "02-02"]
files_modified: [src/main.tsx, src/App.tsx, src/pages/Chat.tsx, src/pages/KnowledgeBase.tsx, src/pages/Settings.tsx, src/components/Layout.tsx, src/components/ProtectedRoute.tsx, package.json]
---

<objective>
Set up React Router with protected route structure and integrate Sidebar + Header into application layout.

Purpose: Enable navigation between pages with authentication guard and consistent layout.
Output: Working multi-page application with routing, protected routes, and integrated Sidebar + Header.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-03-SUMMARY.md
@.planning/phases/02-core-layout-navigation/02-01-SUMMARY.md
@.planning/phases/02-core-layout-navigation/02-02-SUMMARY.md

**Tech stack available:**
- React 18.3.1 + TypeScript 5.6.2
- Tailwind CSS 4.1.18 with custom colors
- Shadcn/UI configured
- Sidebar and Header components ready (from plans 02-01 and 02-02)

**Established patterns:**
- Tailwind v4 CSS-first approach
- Component structure in src/components/
- Path aliases (@/) configured

**Dependencies:**
- Plan 02-01: Sidebar component exists at src/components/Sidebar.tsx
- Plan 02-02: Header component exists at src/components/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React Router</name>
  <files>package.json, package-lock.json</files>
  <action>
Run `npm install react-router-dom` to install React Router v7.x (latest as of 2026). This provides client-side routing capabilities for the SPA. Verify installation completes without errors and package.json is updated with react-router-dom dependency.
  </action>
  <verify>cat package.json | grep react-router-dom shows dependency added</verify>
  <done>React Router installed and verified in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Create page components and Layout wrapper</name>
  <files>src/pages/Chat.tsx, src/pages/KnowledgeBase.tsx, src/pages/Settings.tsx, src/components/Layout.tsx</files>
  <action>
Create four components:

1. **src/pages/Chat.tsx**: Simple page component with heading "Chat" and placeholder text "RAG Agent Chat Interface"
2. **src/pages/KnowledgeBase.tsx**: Simple page component with heading "Knowledge Base" and placeholder text "Browse Documentation"
3. **src/pages/Settings.tsx**: Simple page component with heading "Settings" and placeholder text "Configure API Endpoints"
4. **src/components/Layout.tsx**: Layout wrapper component that:
   - Imports Sidebar and Header components
   - Uses flexbox layout: Sidebar fixed on left (w-64), main content area on right (flex-1)
   - Renders Header at top of content area with dynamic title prop
   - Renders children (page content) below Header with padding
   - Background: main content area has bg-gray-50 (light background)

All pages should be simple TypeScript functional components with proper typing. Layout should accept `children: React.ReactNode` and `title: string` props.
  </action>
  <verify>ls src/pages/ shows Chat.tsx, KnowledgeBase.tsx, Settings.tsx; ls src/components/ shows Layout.tsx</verify>
  <done>Page components and Layout wrapper created with proper structure and TypeScript typing</done>
</task>

<task type="auto">
  <name>Task 3: Set up React Router with protected route structure</name>
  <files>src/main.tsx, src/App.tsx, src/components/ProtectedRoute.tsx</files>
  <action>
1. **Create src/components/ProtectedRoute.tsx**: Component that:
   - Checks mock auth state (hardcoded `const isAuthenticated = true` for now - real auth in Phase 3)
   - Renders children if authenticated
   - Will redirect to login when auth is implemented (add TODO comment)
   - Accepts `children: React.ReactNode` prop

2. **Update src/main.tsx**: Wrap App component with BrowserRouter from react-router-dom

3. **Update src/App.tsx**: Replace current content with Routes configuration:
   - Import Routes, Route from react-router-dom
   - Import Layout, ProtectedRoute, and all page components
   - Define routes:
     - "/" redirects to "/chat" (Navigate from react-router-dom)
     - "/chat" → ProtectedRoute wrapping Layout with Chat page (title="Chat")
     - "/knowledge-base" → ProtectedRoute wrapping Layout with KnowledgeBase page (title="Knowledge Base")
     - "/settings" → ProtectedRoute wrapping Layout with Settings page (title="Settings")
   - Each protected route should render Layout with appropriate title and page component as children

4. **Update Sidebar.tsx navigation**: Replace navigation buttons with react-router-dom Link components:
   - Chat button → `<Link to="/chat">`
   - Knowledge Base button → `<Link to="/knowledge-base">`
   - Settings button → `<Link to="/settings">`
   - Style Link as Button using Shadcn Button's asChild pattern or wrap Link in Button

Use TypeScript with proper types for all components. Follow ESLint/Prettier conventions.
  </action>
  <verify>npm run dev shows working navigation - clicking sidebar items changes page content without full page reload</verify>
  <done>React Router configured, ProtectedRoute guard in place (mock auth), navigation working between Chat/Knowledge Base/Settings pages, Layout renders with Sidebar + Header on all pages</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npx eslint src/` passes with no warnings
- [ ] Navigation works: clicking sidebar items changes page content
- [ ] Layout consistent across all pages (Sidebar + Header always visible)
- [ ] Browser URL updates when navigating (/chat, /knowledge-base, /settings)
- [ ] No console errors or warnings
</verification>

<success_criteria>

- All tasks completed
- React Router installed and configured
- Three page components created (Chat, Knowledge Base, Settings)
- Layout component integrates Sidebar and Header
- ProtectedRoute component guards all pages (mock auth)
- Navigation working between all pages
- Phase 2 complete: Application shell with routing fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-layout-navigation/02-03-SUMMARY.md`:

# Phase 2 Plan 3: React Router Integration Summary

**React Router with protected routes and layout integration complete**

## Accomplishments

- Installed React Router v7.x
- Created three page components: Chat, Knowledge Base, Settings
- Built Layout wrapper integrating Sidebar and Header components
- Implemented ProtectedRoute guard with mock authentication
- Configured routing with Navigate redirect from / to /chat
- Updated Sidebar with working navigation links
- Verified navigation works across all pages

## Files Created/Modified

- `package.json` - Added react-router-dom dependency
- `src/pages/Chat.tsx` - Chat page component
- `src/pages/KnowledgeBase.tsx` - Knowledge Base page component
- `src/pages/Settings.tsx` - Settings page component
- `src/components/Layout.tsx` - Layout wrapper with Sidebar + Header
- `src/components/ProtectedRoute.tsx` - Auth guard component (mock)
- `src/main.tsx` - Added BrowserRouter wrapper
- `src/App.tsx` - Configured Routes with protected pages
- `src/components/Sidebar.tsx` - Updated with Link components for navigation

## Decisions Made

- **Mock authentication**: ProtectedRoute uses hardcoded `isAuthenticated = true` until Phase 3 implements real auth
- **Route structure**: Root "/" redirects to "/chat" as default landing page
- **Layout integration**: Sidebar and Header rendered by Layout wrapper on all protected pages

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 2 complete. Ready for Phase 3: Authentication System (custom Directus login with session management)
</output>
