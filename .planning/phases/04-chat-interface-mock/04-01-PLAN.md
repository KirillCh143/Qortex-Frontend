---
phase: 04-chat-interface-mock
plan: 01
type: execute
depends_on: []
files_modified: [src/pages/Chat.tsx, src/components/MessageBubble.tsx, src/components/ChatInput.tsx]
---

<objective>
Build chat message display and input interface components.

Purpose: Establish the visual foundation for chat interactions with message bubbles and input controls.
Output: Working chat UI that displays messages and accepts user input with mock send functionality.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-layout-navigation/02-03-SUMMARY.md
@.planning/phases/03-authentication-system/03-03-SUMMARY.md
@src/pages/Chat.tsx
@src/components/ui/button.tsx

**Tech stack available:** React Router, Directus SDK, Shadcn/UI (Button, Input, Avatar, Separator)
**Established patterns:** Layout wrapper pattern, color palette (deep-blue background, cyan accents)
**Constraining decisions:**
- Phase 2: Deep-blue (#1e3a8a) background, cyan (#06b6d4) accents for UI consistency
- Phase 3: AuthContext provides user data for message attribution
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create message bubble component</name>
  <files>src/components/MessageBubble.tsx</files>
  <action>Create MessageBubble component accepting props: {role: 'user' | 'assistant', content: string, timestamp?: Date}. User messages align right with cyan background (#06b6d4), assistant messages align left with gray-100 background. Include avatar icon (user: User icon, assistant: Bot icon from lucide-react). Format timestamp as HH:MM if provided. Use Tailwind for styling with rounded corners and padding. Don't add complex animations yet - keep it simple and readable.</action>
  <verify>Component renders without TypeScript errors, accepts all prop types correctly</verify>
  <done>MessageBubble component displays user and assistant messages with distinct styling, proper alignment, and optional timestamps</done>
</task>

<task type="auto">
  <name>Task 2: Build chat input interface and wire up message flow</name>
  <files>src/pages/Chat.tsx, src/components/ChatInput.tsx</files>
  <action>Create ChatInput component with textarea (auto-resize, max 4 rows), Send button (cyan bg, white text), keyboard shortcut (Enter to send, Shift+Enter for newline). Component emits onSend(message: string) callback. Update Chat.tsx to use local state for messages array: [{role, content, timestamp}]. Add ChatInput at bottom, MessageBubble list above with scroll container. On send, add user message to state, then after 500ms delay add mock assistant response "This is a mock response. Real API integration comes in Phase 7." Use flexbox with flex-col to position input at bottom. Disable send button when input is empty.</action>
  <verify>npm run build succeeds, dev server runs without errors, typing and clicking send adds messages to the display</verify>
  <done>Chat page displays message history, accepts input via ChatInput, sends messages on Enter or button click, shows mock assistant responses</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes (or only pre-existing warnings)
- [ ] Chat page renders message bubbles with correct styling
- [ ] User can type and send messages
- [ ] Mock assistant response appears after user message
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors introduced
- Chat UI functional with message display and input
</success_criteria>

<output>
After completion, create `.planning/phases/04-chat-interface-mock/04-01-SUMMARY.md`:

# Phase 4 Plan 1: Chat Message Components Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 04-02-PLAN.md (RAG/LLM mode switching)
</output>
