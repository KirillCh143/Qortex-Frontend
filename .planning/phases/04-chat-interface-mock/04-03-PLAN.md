---
phase: 04-chat-interface-mock
plan: 03
type: execute
depends_on: ["04-02"]
files_modified: [src/pages/Chat.tsx, src/lib/chatStorage.ts]
---

<objective>
Implement message history persistence with localStorage and clear history functionality.

Purpose: Preserve chat conversations across browser sessions and provide users control to clear history.
Output: Chat messages persist in localStorage, restore on page load, with clear history button.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-chat-interface-mock/04-02-SUMMARY.md
@src/pages/Chat.tsx

**Tech stack available:** React Router, Shadcn/UI, localStorage
**Established patterns:** AuthContext pattern for shared functionality
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chat storage utility and restore history on mount</name>
  <files>src/lib/chatStorage.ts, src/pages/Chat.tsx</files>
  <action>Create src/lib/chatStorage.ts with type Message = {role: 'user' | 'assistant', content: string, timestamp: Date, mode?: 'rag' | 'llm'}. Export functions: saveMessages(messages: Message[]): void (stores to localStorage key 'chat-messages' as JSON with date serialization), loadMessages(): Message[] (retrieves from localStorage, parses dates from ISO strings, returns [] if not found or parse error). Update Chat.tsx to call loadMessages() in useEffect on mount, set initial messages state. Update message send logic to call saveMessages after adding new messages to state.</action>
  <verify>Add message, refresh page, messages persist and display correctly with timestamps</verify>
  <done>Messages save to localStorage automatically, restore on page load, dates deserialize correctly</done>
</task>

<task type="auto">
  <name>Task 2: Add clear history button with confirmation</name>
  <files>src/pages/Chat.tsx</files>
  <action>Add "Clear History" button in header area of Chat page (top-right, next to mode toggle). Use Shadcn Button variant="outline" with Trash2 icon from lucide-react. On click, show simple browser confirm dialog: "Clear all chat history? This cannot be undone." If confirmed, clear messages state to [], call saveMessages([]), and optionally add a system message "Chat history cleared" with gray styling. Button should be disabled when messages array is empty.</action>
  <verify>Clicking clear history shows confirmation, accepting clears messages from display and localStorage, rejecting leaves messages intact</verify>
  <done>Clear history button removes all messages from display and localStorage after user confirmation</done>
</task>

<task type="auto">
  <name>Task 3: Add scroll-to-bottom on new message</name>
  <files>src/pages/Chat.tsx</files>
  <action>Add useRef for message container div. Create useEffect that runs when messages.length changes: use ref.current?.scrollTo({top: ref.current.scrollHeight, behavior: 'smooth'}) to auto-scroll to bottom when new message arrives. This ensures latest message is visible after user sends or assistant responds. Don't use external libraries - native scrollTo works fine.</action>
  <verify>Sending message auto-scrolls to show new message at bottom, manual scroll up is not interrupted until next message arrives</verify>
  <done>Chat container auto-scrolls to bottom when new messages arrive, keeping conversation in view</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Messages persist after page refresh
- [ ] Clear history button clears display and localStorage
- [ ] Auto-scroll works when new messages arrive
- [ ] No TypeScript errors
- [ ] Phase 4 fully functional with mock data
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Message persistence works correctly
- Clear history functionality works
- Phase 4 complete with full mock chat experience
</success_criteria>

<output>
After completion, create `.planning/phases/04-chat-interface-mock/04-03-SUMMARY.md`:

# Phase 4 Plan 3: Message History Persistence Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 4 complete. Ready for Phase 5: Knowledge Base Interface
</output>
