---
phase: 04-chat-interface-mock
plan: 02
type: execute
depends_on: ["04-01"]
files_modified: [src/pages/Chat.tsx, src/lib/mockResponses.ts]
---

<objective>
Implement RAG Search and LLM Chat mode switching with mode-specific mock responses.

Purpose: Allow users to toggle between RAG (document search) and LLM (conversational AI) modes with distinct response patterns.
Output: Functional mode switcher with mock responses demonstrating the difference between RAG and LLM modes.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-chat-interface-mock/04-01-SUMMARY.md
@src/pages/Chat.tsx
@src/components/ui/button.tsx

**Tech stack available:** React Router, Shadcn/UI, local state management
**Established patterns:** Color palette (cyan accents), message structure from 04-01
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mode toggle and create mock response generator</name>
  <files>src/pages/Chat.tsx, src/lib/mockResponses.ts</files>
  <action>Add mode state to Chat.tsx: useState<'rag' | 'llm'>('rag'). Create toggle UI above chat input using Shadcn Button group or tabs: "RAG Search" and "LLM Chat" options with active state styling (cyan for active). Create src/lib/mockResponses.ts with generateMockResponse(mode: 'rag' | 'llm', userMessage: string): string. RAG mode returns: "**RAG Search Result:**\n\nBased on your query '[userMessage]', I found relevant information in:\n\n📄 Document: Employee Handbook.pdf\n- Section 3.2: [Mock excerpt related to query]\n\n📄 Document: Technical Guidelines.md\n- Best practices for [topic from query]\n\nWould you like me to retrieve the full documents?" LLM mode returns: "I understand you're asking about [paraphrase userMessage]. Let me help with that...\n\n[2-3 sentences of conversational mock response]\n\nIs there anything specific you'd like me to clarify?" Update message send logic to call generateMockResponse with current mode. Don't use external libraries - just string templates.</action>
  <verify>Build succeeds, toggling between modes changes mock response format, RAG shows document references, LLM shows conversational style</verify>
  <done>Mode toggle switches between RAG and LLM, mock responses reflect mode differences with document citations for RAG and conversational text for LLM</done>
</task>

<task type="auto">
  <name>Task 2: Add mode indicator to message bubbles</name>
  <files>src/components/MessageBubble.tsx</files>
  <action>Update MessageBubble props to accept optional mode: 'rag' | 'llm'. For assistant messages, display small badge in top-right of bubble showing mode: RAG (with FileSearch icon) or LLM (with MessageSquare icon) from lucide-react. Badge has subtle gray-200 background, small text. Position absolute in bubble's relative container. Only show for assistant messages, not user messages.</action>
  <verify>Assistant messages display mode badge, user messages don't show badge, icons render correctly</verify>
  <done>Assistant message bubbles show mode indicator badge, making it clear which mode generated each response</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Mode toggle switches between RAG and LLM
- [ ] RAG responses show document references and citations
- [ ] LLM responses show conversational text
- [ ] Mode badge appears on assistant messages
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Mode switching works correctly
- Mock responses demonstrate RAG vs LLM differences
</success_criteria>

<output>
After completion, create `.planning/phases/04-chat-interface-mock/04-02-SUMMARY.md`:

# Phase 4 Plan 2: Mode Switching Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 04-03-PLAN.md (message history persistence)
</output>
