---
phase: 07-api-integration-layer
plan: 01
type: execute
depends_on: []
files_modified: [package.json, src/main.tsx, src/providers/QueryProvider.tsx]
---

<objective>
Set up React Query infrastructure with development tools and default retry configuration.

Purpose: Establish the foundation for API state management, retry logic, and caching that all subsequent API integrations depend on.
Output: React Query installed and configured with QueryProvider wrapping the app and devtools enabled.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-api-integration-layer/07-RESEARCH.md
@src/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React Query dependencies</name>
  <files>package.json</files>
  <action>Install @tanstack/react-query v5.x, @tanstack/react-query-devtools, and zod v3.x using npm install. These are the standard stack per RESEARCH.md - React Query for API state management with built-in retry/caching, Zod for runtime validation. Do NOT install axios (not needed - using fetch with SDK/React Query).</action>
  <verify>npm list @tanstack/react-query @tanstack/react-query-devtools zod shows all three packages installed</verify>
  <done>package.json contains @tanstack/react-query, @tanstack/react-query-devtools (dev), and zod in dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create QueryProvider with retry configuration</name>
  <files>src/providers/QueryProvider.tsx</files>
  <action>Create QueryProvider component following RESEARCH.md code example (React Query Setup with Error Handling section). Configure QueryClient with: retry: 3, retryDelay with exponential backoff (Math.min(1000 * 2 ** attemptIndex, 30000)), staleTime: 5 * 60 * 1000 (5 minutes), refetchOnWindowFocus: false. Include ReactQueryDevtools with initialIsOpen: false. This matches the standard pattern from RESEARCH.md - don't hand-roll retry logic, use React Query's built-in configuration.</action>
  <verify>File exists at src/providers/QueryProvider.tsx, exports QueryProvider component, includes QueryClientProvider and ReactQueryDevtools</verify>
  <done>QueryProvider.tsx created with QueryClient configured per RESEARCH.md defaults, ready to wrap app</done>
</task>

<task type="auto">
  <name>Task 3: Wrap App with QueryProvider</name>
  <files>src/main.tsx</files>
  <action>Import QueryProvider from @/providers/QueryProvider and wrap it AROUND the existing RouterProvider (QueryProvider > RouterProvider > App tree). This ensures React Query context is available throughout the app. Place it at the same level as StrictMode, not inside AuthProvider (QueryProvider and AuthProvider are independent concerns).</action>
  <verify>npm run build succeeds without errors, src/main.tsx imports QueryProvider and wraps RouterProvider</verify>
  <done>QueryProvider wraps the app, React Query devtools accessible in development, build passes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm list shows @tanstack/react-query, @tanstack/react-query-devtools, zod installed
- [ ] npm run build succeeds without TypeScript errors
- [ ] QueryProvider.tsx exists with QueryClient configured per RESEARCH.md
- [ ] main.tsx wraps RouterProvider with QueryProvider
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- React Query infrastructure ready for Plans 02 and 03
- No TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/07-api-integration-layer/07-01-SUMMARY.md`:

# Phase 7 Plan 1: React Query Setup Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 07-02-PLAN.md and 07-03-PLAN.md (can run in parallel)
</output>
