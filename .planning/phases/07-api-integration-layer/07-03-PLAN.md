---
phase: 07-api-integration-layer
plan: 03
type: execute
depends_on: ["07-01"]
files_modified: [src/services/directus/files.service.ts, src/services/directus/types.ts, src/services/mock/files.mock.ts, src/hooks/useFiles.ts, src/pages/KnowledgeBase.tsx, src/lib/config.ts]
---

<objective>
Build Directus Files API integration with mock/real toggle and connect KnowledgeBase page to use service layer instead of mock data.

Purpose: Enable real document browsing from Directus Files collection while maintaining mock mode for development/testing.
Output: KnowledgeBase interface seamlessly switches between mock documents and real Directus files based on configuration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-api-integration-layer/07-RESEARCH.md
@.planning/phases/07-api-integration-layer/07-01-SUMMARY.md
@src/pages/KnowledgeBase.tsx
@src/lib/mockDocuments.ts
@src/lib/directus.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Directus files service with mock/real implementations</name>
  <files>src/services/directus/files.service.ts, src/services/directus/types.ts, src/services/mock/files.mock.ts</files>
  <action>Create three files following RESEARCH.md Files Service pattern:

1. **types.ts**: Define DirectusFile interface matching RESEARCH.md schema (id, filename_download, type, filesize, title, description, uploaded_on). Define FilesService interface with getFiles(params?: {limit?: number, search?: string}) and downloadFile(id: string) methods.

2. **files.service.ts**: Create createRealFilesService(client: DirectusClient) returning FilesService. Import readFiles, readFile from '@directus/sdk/rest'. In getFiles, use client.request(readFiles({ limit, search if provided, sort: ['-uploaded_on'], fields: [...] })). In downloadFile, fetch from ${client.url}/assets/${id} and return response.blob(). Follow RESEARCH.md example exactly - don't hand-roll asset URL construction.

3. **files.mock.ts**: Create createMockFilesService() returning FilesService. Reuse mockDocuments from src/lib/mockDocuments.ts - map Document type to DirectusFile type (id stays id, title→title, description→description, etc.). In getFiles, filter by search if provided, return slice(0, limit). In downloadFile, call generateMockFileContent (from mockDocuments.ts) and return blob.

All three files export same FilesService interface for type safety.</action>
  <verify>Files exist at expected paths, types.ts exports DirectusFile and FilesService interfaces, both services implement same interface</verify>
  <done>Files services created with DirectusFile types, mock/real implementations, consistent interface</done>
</task>

<task type="auto">
  <name>Task 2: Update config module and create React Query hooks</name>
  <files>src/lib/config.ts, src/hooks/useFiles.ts</files>
  <action>Update and create two files:

1. **config.ts** (update): Add createFilesService factory. If useMockData, return createMockFilesService(). Else return createRealFilesService(directusClient) - import directusClient from '@/lib/directus'. Export filesService singleton from factory. This matches RESEARCH.md factory pattern with environment-based toggle.

2. **useFiles.ts** (create): Export two hooks following RESEARCH.md React Query Hook pattern:
   - useFiles(params?: {search?: string}): useQuery with queryKey: ['files', params], queryFn: () => filesService.getFiles(params), staleTime from QueryProvider defaults
   - useDownloadFile(): useMutation with mutationFn: (fileId: string) => filesService.downloadFile(fileId), onSuccess creates blob URL and triggers browser download via temp <a> element, then revokes URL (matches existing pattern from KnowledgeBase.tsx handleDownload)

Import filesService from config.ts.</action>
  <verify>config.ts exports filesService, useFiles.ts exports both hooks with correct React Query usage</verify>
  <done>Config module extended with files factory, React Query hooks for files queries and downloads</done>
</task>

<task type="auto">
  <name>Task 3: Update KnowledgeBase page to use files service</name>
  <files>src/pages/KnowledgeBase.tsx</files>
  <action>Replace mockDocuments with useFiles hook. Import useFiles and useDownloadFile from '@/hooks/useFiles'. Remove useState for documents - replace with const { data: files = [], isLoading, error } = useFiles({ search: searchQuery }). Remove useEffect for filtering (React Query handles this via queryKey change). Update filteredDocuments references to use files directly. Replace handleView and handleDownload to use useDownloadFile mutation - call downloadMutation.mutate(selectedDoc.id) in both. For view, in onSuccess, open blob URL in new tab then revoke after 100ms (existing pattern). Keep selectedDoc state and detail panel UI unchanged. Handle loading state (show spinner or skeleton). Handle error state (show error message). Remove imports of mockDocuments, formatFileSize, formatDate, generateMockFileContent (service layer handles this now).</action>
  <verify>npm run build succeeds, KnowledgeBase.tsx no longer imports mockDocuments, uses useFiles hook instead</verify>
  <done>KnowledgeBase page integrated with files service, handles loading/error states, detail panel unchanged</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without TypeScript errors
- [ ] Mock mode returns mockDocuments mapped to DirectusFile type
- [ ] KnowledgeBase handles loading/error states from query
- [ ] Download mutation triggers browser download correctly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- KnowledgeBase uses files service layer (mock or real based on VITE_USE_MOCK_DATA)
- Type-safe with DirectusFile interface
- No TypeScript errors
- Phase 7 complete
  </success_criteria>

<output>
After completion, create `.planning/phases/07-api-integration-layer/07-03-SUMMARY.md`:

# Phase 7 Plan 3: Directus Files Integration Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 7 complete, ready for Phase 8 (Polish & Testing)
</output>
