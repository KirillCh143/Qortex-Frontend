---
phase: 07-api-integration-layer
plan: 02
type: execute
depends_on: ["07-01"]
files_modified: [src/services/n8n/webhook.service.ts, src/services/n8n/types.ts, src/services/mock/webhook.mock.ts, src/hooks/useChatQuery.ts, src/pages/Chat.tsx, src/lib/config.ts]
---

<objective>
Build n8n webhook integration with mock/real toggle and connect Chat page to use service layer instead of mock data.

Purpose: Enable real AI-powered chat queries via n8n webhook while maintaining mock mode for development/testing.
Output: Chat interface seamlessly switches between mock responses and real n8n webhook calls based on configuration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-api-integration-layer/07-RESEARCH.md
@.planning/phases/07-api-integration-layer/07-01-SUMMARY.md
@src/pages/Chat.tsx
@src/lib/mockResponses.ts
@src/lib/settings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create n8n webhook service with Zod validation</name>
  <files>src/services/n8n/webhook.service.ts, src/services/n8n/types.ts, src/services/mock/webhook.mock.ts</files>
  <action>Create three files following RESEARCH.md n8n Webhook Service pattern:

1. **types.ts**: Define Zod schemas - ChatQueryPayloadSchema (question: string, mode: 'rag'|'llm', sessionId: string, history: optional array), WebhookResponseSchema (answer: string, sources: optional array with title/url/relevance). Export inferred types using z.infer.

2. **webhook.service.ts**: Create createWebhookService(webhookUrl) returning { sendQuery } method. Use fetch (not axios - no dependency needed) with timeout 95000ms (RESEARCH.md recommendation: just under n8n's 100s limit). Validate outgoing payload with ChatQueryPayloadSchema.parse(). Validate response with WebhookResponseSchema.parse(). Handle timeout errors with user-friendly message per RESEARCH.md error handling example. Use AbortController for timeout.

3. **webhook.mock.ts**: Create createMockWebhookService() with same interface. Return delayed mock response (500ms setTimeout) that echoes question in answer with mock sources. Reuse logic from existing generateMockResponse() in src/lib/mockResponses.ts but adapt to new interface.

Do NOT use axios (avoid extra dependency). Use native fetch with AbortController for timeout handling.</action>
  <verify>Files exist at expected paths, types.ts exports ChatQueryPayload and WebhookResponse types, both services export same interface signature</verify>
  <done>Webhook services created with Zod validation, timeout handling, mock/real implementations, same interface</done>
</task>

<task type="auto">
  <name>Task 2: Create config module and React Query hook</name>
  <files>src/lib/config.ts, src/hooks/useChatQuery.ts</files>
  <action>Create two files:

1. **config.ts**: Export useMockData boolean derived from import.meta.env.VITE_USE_MOCK_DATA === 'true' (matches RESEARCH.md pattern). Export createWebhookService factory that checks useMockData - if true, return createMockWebhookService(), else return createWebhookService(webhookUrl from settings). Import loadSettings() to get n8n webhook URL from user settings.

2. **useChatQuery.ts**: Create useChatQuery() hook using useMutation from React Query (NOT useQuery - chat queries are mutations, not cached queries). mutationFn calls webhookService.sendQuery(). No retry for mutations (default retry: 0 for mutations in QueryProvider config is correct - chat queries shouldn't auto-retry). Return mutation object with mutate, isLoading, error, data. Follow RESEARCH.md React Query Hook pattern but adapted for mutation instead of query.

Import webhookService from config.ts factory.</action>
  <verify>config.ts exports useMockData and webhookService, useChatQuery.ts exports hook that returns mutation object</verify>
  <done>Config module with factory pattern, React Query mutation hook for chat queries</done>
</task>

<task type="auto">
  <name>Task 3: Update Chat page to use webhook service</name>
  <files>src/pages/Chat.tsx</files>
  <action>Replace generateMockResponse() with useChatQuery hook. Import useChatQuery. Call const chatMutation = useChatQuery() at component level. In handleSend, replace setTimeout with generateMockResponse logic with: chatMutation.mutate({ question: content, mode, sessionId: crypto.randomUUID(), history: messages.slice(-10) }). In onSuccess callback, add assistant message with mutation.data.answer. Handle loading state during mutation.isLoading (show typing indicator or disable input). Handle error with mutation.error (show error message to user). Remove import of generateMockResponse. Keep all existing message persistence logic unchanged (that's working correctly from Phase 4/6).</action>
  <verify>npm run build succeeds, Chat.tsx no longer imports generateMockResponse, uses useChatQuery hook instead</verify>
  <done>Chat page integrated with webhook service, handles loading/error states, message persistence unchanged</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without TypeScript errors
- [ ] Zod schemas validate payloads at runtime
- [ ] Mock mode returns responses after 500ms delay
- [ ] Chat page handles loading/error states from mutation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Chat page uses webhook service layer (mock or real based on VITE_USE_MOCK_DATA)
- Type-safe payloads with runtime validation
- No TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/07-api-integration-layer/07-02-SUMMARY.md`:

# Phase 7 Plan 2: n8n Webhook Integration Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for Phase 7 completion (07-03 may run in parallel)
</output>
