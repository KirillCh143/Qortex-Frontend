---
phase: 7.3-directory-structure-integration-sidebar-and-navigation
plan: 02
type: execute
depends_on: ["7.3-01"]
files_modified: [src/pages/KnowledgeBase.tsx, src/components/FileListView.tsx]
---

<objective>
Complete Knowledge Base UI with List/Grid view toggle, folder sidebar integration, and refined file actions.

Purpose: Provide flexible document browsing experience with folder navigation and multiple view modes, while simplifying file actions to focus on download functionality.
Output: Enhanced Knowledge Base page with 3-column layout (folders | files | details), view toggle, and streamlined download-only file actions.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/7.3-directory-structure-integration-sidebar-and-navigation/7.3-01-SUMMARY.md
@src/pages/KnowledgeBase.tsx
@src/components/FolderTree.tsx
@src/hooks/useFolders.ts
@src/hooks/useFiles.ts

**From Plan 1:**
- FolderTree component with folder selection
- useFolders hook for fetching folder hierarchy
- Updated useFiles hook supports folder filtering

**Current state:**
- KnowledgeBase.tsx has grid view with Card components
- Detail panel has View and Download buttons
- Download functionality already implemented and working
- View button opens file in new tab using blob URL

**Requirements:**
- Add List view option (table-style layout)
- Add view toggle buttons (List/Grid icons)
- Integrate FolderTree in left sidebar
- Show folder name and file count in header
- Remove View button (Eye icon), keep only Download
- Ensure Download uses correct filename from selectedDoc.filename_download
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement List/Grid view toggle</name>
  <files>src/pages/KnowledgeBase.tsx, src/components/FileListView.tsx</files>
  <action>
1. In KnowledgeBase.tsx:
   - Add viewMode state: useState<'grid' | 'list'>('grid')
   - Import LayoutGrid and List icons from lucide-react
   - Add toggle buttons in header (after search bar, before file list):
     * Two Button components with variant="outline"
     * LayoutGrid icon for grid view, List icon for list view
     * Apply cyan border and bg when active: border-cyan-500 bg-cyan-50 when viewMode matches
     * Click handler sets viewMode
   - Conditionally render based on viewMode:
     * viewMode === 'grid': existing Card grid layout
     * viewMode === 'list': FileListView component (to be created)

2. Create components/FileListView.tsx:
   - Accept props: { files: DirectusFile[], onSelectFile: (file: DirectusFile) => void }
   - Render table with columns: Title, Type, Size, Uploaded
   - Use table element with Tailwind classes: table-auto w-full
   - Header row with gray-100 bg, font-medium text-sm
   - Data rows with hover:bg-gray-50, cursor-pointer, border-b border-gray-100
   - Click row to call onSelectFile (opens detail panel)
   - Use formatFileSize and formatDate helpers from mockDocuments
   - Show file type badge similar to grid view (rounded gray badge)
   - Add subtle Download icon in last column (no click handler - row click opens panel)

Use semantic HTML table, not divs. Ensure table is responsive (horizontal scroll on small screens with overflow-x-auto wrapper). List view should feel like a file manager, grid view like a gallery.
  </action>
  <verify>npm run dev, toggle between list and grid views, verify both display files correctly, check that clicking file in list view opens detail panel, verify table styling matches design system</verify>
  <done>View toggle buttons functional, grid view shows cards, list view shows table, both views allow file selection, table is responsive and accessible</done>
</task>

<task type="auto">
  <name>Task 2: Integrate folder sidebar and refine detail panel</name>
  <files>src/pages/KnowledgeBase.tsx</files>
  <action>
1. Add folder state and filtering:
   - Import useFolders hook
   - Import FolderTree component
   - Add selectedFolderId state: useState<string | null>(null)
   - Fetch folders: const { data: folders = [] } = useFolders()
   - Update useFiles call to include folder filter: useFiles({ search: searchQuery, folder: selectedFolderId })
   - Handle folder selection: setSelectedFolderId callback

2. Update layout structure:
   - Change root div to flex layout with sidebar
   - Left sidebar (w-64, border-r, bg-gray-50): FolderTree component
     * Pass folders, selectedFolderId, onSelectFolder props
     * Fixed width on desktop, hidden on mobile (md:block hidden)
   - Main content area (flex-1): existing search + file list + detail panel
   - Ensure detail panel overlays on top (z-50) same as before

3. Update header:
   - Add folder context above search bar
   - Show folder name: "All Documents" if selectedFolderId is null, otherwise find folder.name from folders array
   - Show file count: "X documents" using files.length
   - Use text-sm text-gray-600 for metadata
   - Keep h1 "Knowledge Base" but add folder breadcrumb below it

4. Refine detail panel actions:
   - **REMOVE the View button entirely** (delete Button with Eye icon and handleView function)
   - Keep only Download button
   - Download button should remain full width (was flex-1 before)
   - Verify handleDownload uses selectedDoc.filename_download for download attribute
   - Keep Download button variant="outline" with Download icon

The folder sidebar should visually separate navigation from content. Use border-r and subtle bg-gray-50 for sidebar. Detail panel stays as is (overlay + fixed panel). DO NOT add Preview/View button - only Download should remain.
  </action>
  <verify>npm run dev, verify folder sidebar appears on left, click folders to filter files, check header shows correct folder name and count, verify detail panel only has Download button (no View/Eye button), test download works with correct filename</verify>
  <done>FolderTree integrated in left sidebar, files filter by selected folder, header shows folder context and file count, detail panel has only Download button, download uses correct filename</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Knowledge Base UI with folder navigation, List/Grid view toggle, and download-only file actions</what-built>
  <how-to-verify>
1. Run: npm run dev
2. Navigate to: http://localhost:5173/knowledge-base (after login)
3. Test folder navigation:
   - Click different folders in left sidebar
   - Verify file list updates to show only files in selected folder
   - Check "All Documents" shows all files
   - Verify folder expand/collapse works correctly
4. Test view toggle:
   - Click List view icon (should show table layout)
   - Click Grid view icon (should show card layout)
   - Verify both views display file information correctly
5. Test header display:
   - Check folder name shows in header
   - Verify file count is accurate ("X documents")
   - Check count updates when switching folders
6. Test file detail panel:
   - Click a file in either view mode
   - Verify detail panel opens from right
   - **Confirm there is NO "View" button (Eye icon) - only Download button should exist**
   - Click Download button
   - Verify file downloads with correct filename (not just file ID)
   - Check downloaded file opens correctly
7. Test responsive behavior:
   - Resize browser to mobile width
   - Verify folder sidebar hides on mobile
   - Check file list and detail panel work on mobile
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] View toggle switches between list and grid layouts
- [ ] FileListView component renders table correctly
- [ ] FolderTree appears in left sidebar
- [ ] Files filter by selected folder
- [ ] Header shows folder name and file count
- [ ] Detail panel has ONLY Download button (no View button)
- [ ] Download uses selectedDoc.filename_download
- [ ] Layout is responsive (sidebar hidden on mobile)
- [ ] User approval received via checkpoint
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Folder navigation functional with visual feedback
- List/Grid views both working and styled correctly
- Detail panel streamlined to Download-only
- Download functionality verified working
- User has approved UI in checkpoint
- Phase 7.3 complete - Directory structure integration successful
  </success_criteria>

<output>
After completion, create `.planning/phases/7.3-directory-structure-integration-sidebar-and-navigation/7.3-02-SUMMARY.md`:

# Phase 7.3 Plan 2: View Toggle & Layout Refinement Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- Implemented List/Grid view toggle with icons
- Created FileListView component for table-style display
- Integrated FolderTree into Knowledge Base left sidebar
- Added folder context to header (name + file count)
- Removed View button from detail panel (Download-only)
- Verified download functionality uses correct filename

## Files Created/Modified

- `src/pages/KnowledgeBase.tsx` - Added 3-column layout, folder sidebar, view toggle, updated detail panel
- `src/components/FileListView.tsx` - Created table view component

## Decisions Made

- List view uses semantic table element (not divs) for accessibility
- Folder sidebar fixed width on desktop (w-64), hidden on mobile
- Detail panel simplified to Download-only per roadmap requirements
- View toggle buttons use outline variant with cyan accent when active

## Issues Encountered

[Problems and resolutions, or "None"]

## Deviations from Plan

[Any changes from original plan, or "None"]

## Next Phase Readiness

**Phase 7.3 Complete** - Directory structure integration successful. Knowledge Base now supports folder hierarchy navigation, multiple view modes, and streamlined file actions.

**Ready for:** Phase 8 (Polish & Testing) - End-to-end testing and UI refinement

---
*Phase: 7.3-directory-structure-integration-sidebar-and-navigation*
*Plan: 02*
*Completed: [date]*
</output>
