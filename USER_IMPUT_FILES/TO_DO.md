# 1) Redirect from login page if auth is OK

2. Double check if lock on redis is needed (create 5 files, start, while processing create 5 new files, see if queuing works)
3. All settings on settings page must be configured once at the system level and apply to all users, not stored per browser (liek now).
